# Run with `dart run ffigen --config ffigen.yaml`.
name: NitroBindings
description: |
  Bindings for `src/nitro.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/nitro_bindings_generated.dart'
headers:
  entry-points:
    - 'src/nitro.h'
  include-directives:
    - 'src/nitro.h'
comments:
  style: any
  length: full
