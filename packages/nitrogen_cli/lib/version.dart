/// Must match `pubspec.yaml`; `test/version_test.dart` enforces it.
const String nitrogenVersion = '0.7.3';

/// The version reported by `nitrogen --version` and the doctor/dashboard
/// headers.
const String activeVersion = nitrogenVersion;
