// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.3
// Generated from: web_echo.native.dart
// NativeImpl: cpp — shared C++ virtual-dispatch bridge (no JNI / Swift).
#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>
#include <string>
#include <stdexcept>
#ifdef __EMSCRIPTEN__
#include "nitro_wasm_compat.h"
#else
#include "dart_api_dl.h"
#endif
#include "web_echo.bridge.g.h"
#include "web_echo.native.g.h"

extern "C" {
NITRO_EXPORT uint32_t web_echo_nitro_abi_version(void) {
    return 1;
}
NITRO_EXPORT const char* web_echo_nitro_bridge_checksum(void) {
    return "5847a5853ed875e5";
}
NITRO_EXPORT intptr_t web_echo_init_dart_api_dl(void* data) {
    return Dart_InitializeApiDL(data);
}
#ifdef __EMSCRIPTEN__
// Web replacement for Dart_PostCObject: Dart registers a function-table
// callback here at module load (see nitro_wasm_compat.h for the envelope).
NITRO_EXPORT __attribute__((weak)) void web_echo_nitro_set_post_fn(NitroPostFn fn) {
    g_nitro_post_fn = fn;
}
#endif
}

static thread_local NitroError g_nitro_error = { 0, nullptr, nullptr, nullptr, nullptr };
alignas(8) static thread_local uint8_t _g_opt_ret[16];
static thread_local std::string _g_str_ret;

extern "C" {
NitroError* web_echo_get_error() { return &g_nitro_error; }
void web_echo_clear_error() {
    if (!g_nitro_error.hasError) { return; }
    g_nitro_error.hasError = 0;
    if (g_nitro_error.name)       { free((void*)g_nitro_error.name);       g_nitro_error.name       = nullptr; }
    if (g_nitro_error.message)    { free((void*)g_nitro_error.message);    g_nitro_error.message    = nullptr; }
    if (g_nitro_error.code)       { free((void*)g_nitro_error.code);       g_nitro_error.code       = nullptr; }
    if (g_nitro_error.stackTrace) { free((void*)g_nitro_error.stackTrace); g_nitro_error.stackTrace = nullptr; }
}
static void nitro_report_error(const char* name, const char* message, const char* code, const char* stack) {
    web_echo_clear_error();
    g_nitro_error.hasError = 1;
    g_nitro_error.name       = name    ? strdup(name)    : strdup("NativeException");
    g_nitro_error.message    = message ? strdup(message) : strdup("An unknown C++ exception occurred.");
    g_nitro_error.code       = code    ? strdup(code)    : nullptr;
    g_nitro_error.stackTrace = stack   ? strdup(stack)   : nullptr;
}
}

#include <atomic>
#include <memory>
#include <mutex>
#include <unordered_map>

using HybridWebEchoFactory = std::function<std::shared_ptr<HybridWebEcho>(const std::string&)>;
static std::unordered_map<int64_t, std::shared_ptr<HybridWebEcho>>& _g_instances() {
    static std::unordered_map<int64_t, std::shared_ptr<HybridWebEcho>> m;
    return m;
}
static std::mutex& _g_instances_mtx() {
    static std::mutex m;
    return m;
}
static std::atomic<int64_t>& _g_next_instance_id() {
    static std::atomic<int64_t> id{1};
    return id;
}
static HybridWebEchoFactory& _g_factory() {
    static HybridWebEchoFactory f;
    return f;
}

static constexpr int _kNitroCacheWays = 8;
struct alignas(64) _NitroInstSlot {
    std::atomic<int64_t> id{-1};
    std::atomic<HybridWebEcho*> ptr{nullptr};
};
static _NitroInstSlot _g_inst_cache[_kNitroCacheWays];
static HybridWebEcho* _nitro_get_instance(int64_t id) {
    _NitroInstSlot& _slot = _g_inst_cache[(uint64_t)id & (_kNitroCacheWays - 1)];
    if (_slot.id.load(std::memory_order_acquire) == id) {
        return _slot.ptr.load(std::memory_order_relaxed);
    }
    std::lock_guard<std::mutex> _lk(_g_instances_mtx());
    auto it = _g_instances().find(id);
    HybridWebEcho* p = (it != _g_instances().end()) ? it->second.get() : nullptr;
    if (p) {
        _slot.ptr.store(p, std::memory_order_relaxed);
        _slot.id.store(id, std::memory_order_release);
    }
    return p;
}
static void _nitro_invalidate_cache() {
    for (int _i = 0; _i < _kNitroCacheWays; ++_i) {
        _g_inst_cache[_i].id.store(-1, std::memory_order_release);
        _g_inst_cache[_i].ptr.store(nullptr, std::memory_order_relaxed);
    }
}

extern "C" {
void web_echo_register_factory(void* fn) {
    _g_factory() = *static_cast<HybridWebEchoFactory*>(fn);
}
void web_echo_register_impl(HybridWebEcho* impl) {
    std::lock_guard<std::mutex> _lk(_g_instances_mtx());
    if (impl) _g_instances()[0] = std::shared_ptr<HybridWebEcho>(impl, [](HybridWebEcho*){});
    else _g_instances().erase(0);
    _nitro_invalidate_cache();
}
HybridWebEcho* web_echo_get_impl() {
    return _nitro_get_instance(0);
}
NITRO_EXPORT int64_t web_echo_create_instance(const char* key) {
    if (_g_factory()) {
        auto inst = _g_factory()(key ? key : "");
        if (!inst) return -1;
        std::lock_guard<std::mutex> _lk(_g_instances_mtx());
        int64_t id = ++_g_next_instance_id();
        _g_instances()[id] = std::move(inst);
        return id;
    }
    // Legacy: no factory registered — return 0 (single global impl slot)
    return 0;
}
NITRO_EXPORT void web_echo_destroy_instance(int64_t instanceId) {
    std::lock_guard<std::mutex> _lk(_g_instances_mtx());
    _g_instances().erase(instanceId);
    _nitro_invalidate_cache();
}
NITRO_EXPORT void web_echo_nitro_free(void* ptr) { if (ptr) { free(ptr); } }
NITRO_EXPORT void* web_echo_nitro_alloc(size_t size) { return malloc(size); }
NITRO_EXPORT void web_echo_release_typed_data_return(void* ptr) {
    if (!ptr) { return; }
    int64_t* words = (int64_t*)ptr;
    void* payload = (void*)(intptr_t)words[1];
    if (payload && payload != ptr) { free(payload); }
    free(ptr);
}
}

#include <algorithm>
#include <mutex>
#include <vector>

// One registry per stream: every concurrent Dart subscriber registers
// its own ReceivePort. Guarded by a mutex — register/release run on the
// Dart isolate thread while emits may come from any impl thread.
struct _NitroStreamPorts {
    std::mutex mtx;
    std::vector<int64_t> ports;
    void add(int64_t p) { std::lock_guard<std::mutex> l(mtx); ports.push_back(p); }
    void remove(int64_t p) { std::lock_guard<std::mutex> l(mtx); ports.erase(std::remove(ports.begin(), ports.end(), p), ports.end()); }
    std::vector<int64_t> snapshot() { std::lock_guard<std::mutex> l(mtx); return ports; }
};
static _NitroStreamPorts g_ports_ticks;

void HybridWebEcho::emit_ticks(int64_t item) {
    auto _ports = g_ports_ticks.snapshot();
    if (_ports.empty()) { return; }
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = item;
    for (int64_t _port : _ports) {
        if (!Dart_PostCObject_DL(_port, &obj)) { g_ports_ticks.remove(_port); }
    }
}

// S8 helper — writes error info to the out-param slot.
static void _nitro_out_err(NitroError* e, const char* name, const char* msg) {
    if (!e) return;
    e->hasError = 1;
    e->name       = name ? strdup(name) : nullptr;
    e->message    = msg  ? strdup(msg)  : nullptr;
    e->code       = nullptr;
    e->stackTrace = nullptr;
}

extern "C" {

double web_echo_add_double(int64_t instanceId, double a, double b, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return 0.0; }
    try {
        return _impl->addDouble(a, b);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0.0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0.0;
    }
}

int64_t web_echo_add_int(int64_t instanceId, int64_t a, int64_t b, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return 0; }
    try {
        return _impl->addInt(a, b);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

int8_t web_echo_negate(int64_t instanceId, int8_t v, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return false; }
    try {
        return _impl->negate(v);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return false;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return false;
    }
}

const char* web_echo_concat(int64_t instanceId, const char* a, const char* b, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        std::string _res = _impl->concat(std::string(a), std::string(b));
        _g_str_ret = _res; return const_cast<char*>(_g_str_ret.c_str());
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

uint8_t* web_echo_echo_nullable_int(int64_t instanceId, const uint8_t* v, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        std::optional<int64_t> _opt_v;
        if (v != nullptr) {
            auto* _s = reinterpret_cast<const NitroOptInt64*>(v);
            if (_s->hasValue) _opt_v = _s->value;
        }
        auto _opt = _impl->echoNullableInt(_opt_v);
        auto* _out = (NitroOptInt64*)(void*)_g_opt_ret;
        _out->hasValue = _opt.has_value() ? 1 : 0;
        _out->value    = _opt.value_or(0);
        return (uint8_t*)_out;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

int64_t web_echo_echo_enum(int64_t instanceId, int64_t v, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return 0; }
    try {
        return static_cast<int64_t>(_impl->echoEnum(static_cast<EchoLevel>(v)));
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

uint8_t* web_echo_echo_bytes(int64_t instanceId, uint8_t* data, size_t data_length, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _res = _impl->echoBytes(data, static_cast<size_t>(data_length));
        if (_res.size > (size_t)INT64_MAX || (_res.size > 0 && _res.data == nullptr)) {
            nitro_report_error("ArgumentError", "echoBytes: @zeroCopy return buffer has invalid data/size", nullptr, nullptr);
            return nullptr;
        }
        int64_t* _env = (int64_t*)malloc(sizeof(int64_t) * 3);
        if (_env == nullptr) {
            nitro_report_error("OutOfMemoryError", "echoBytes: failed to allocate zero-copy return envelope", nullptr, nullptr);
            return nullptr;
        }
        _env[0] = (int64_t)_res.size;
        _env[1] = (int64_t)(intptr_t)(_res.data != nullptr ? _res.data : (const uint8_t*)_env);
        _env[2] = 0;
        return (uint8_t*)_env;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

uint8_t* web_echo_echo_int32s(int64_t instanceId, int32_t* data, size_t data_length, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _res = _impl->echoInt32s(data, static_cast<size_t>(data_length));
        if (_res.size > (size_t)INT64_MAX || (_res.size > 0 && _res.data == nullptr)) {
            nitro_report_error("ArgumentError", "echoInt32s: @zeroCopy return buffer has invalid data/size", nullptr, nullptr);
            return nullptr;
        }
        int64_t* _env = (int64_t*)malloc(sizeof(int64_t) * 3);
        if (_env == nullptr) {
            nitro_report_error("OutOfMemoryError", "echoInt32s: failed to allocate zero-copy return envelope", nullptr, nullptr);
            return nullptr;
        }
        _env[0] = (int64_t)_res.size;
        _env[1] = (int64_t)(intptr_t)(_res.data != nullptr ? _res.data : (const uint8_t*)_env);
        _env[2] = 0;
        return (uint8_t*)_env;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void* web_echo_echo_stat(int64_t instanceId, void* v, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _buf_v = { (const uint8_t*)v + 4, (size_t)*(int32_t*)v };
        NitroCppBuffer _res = _impl->echoStat(_buf_v);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

uint8_t* web_echo_increment_values(int64_t instanceId, uint8_t* m, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _buf_m = { (const uint8_t*)m + 4, (size_t)*(int32_t*)m };
        NitroCppBuffer _res = _impl->incrementValues(_buf_m);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void* web_echo_echo_stats(int64_t instanceId, void* v, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _buf_v = { (const uint8_t*)v + 4, (size_t)*(int32_t*)v };
        NitroCppBuffer _res = _impl->echoStats(_buf_v);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void* web_echo_echo_ints(int64_t instanceId, void* v, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _buf_v = { (const uint8_t*)v + 4, (size_t)*(int32_t*)v };
        NitroCppBuffer _res = _impl->echoInts(_buf_v);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void* web_echo_echo_maybe_stats(int64_t instanceId, void* v, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _buf_v = { nullptr, 0 };
        if (v != nullptr) {
            _buf_v.data = (const uint8_t*)v + 4;
            _buf_v.size = (size_t)*(int32_t*)v;
        }
        NitroCppBuffer _res = _impl->echoMaybeStats(_buf_v);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void* web_echo_echo_bag(int64_t instanceId, void* v, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _buf_v = { (const uint8_t*)v + 4, (size_t)*(int32_t*)v };
        NitroCppBuffer _res = _impl->echoBag(_buf_v);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void web_echo_always_throws(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return; }
    try {
        _impl->alwaysThrows();
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
    }
}

int64_t web_echo_sum_to(int64_t instanceId, int64_t n) {
    NitroError* _nitro_err = nullptr; // async: errors use TLS not out-param
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return 0; }
    try {
        return _impl->sumTo(n);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

void web_echo_native_async_echo(int64_t instanceId, int64_t value, NitroError* _nitro_err, int64_t dart_port) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) {
        _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered.");
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
        return;
    }
    try {
        _impl->nativeAsyncEcho(value, _nitro_err, dart_port);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
    }
}

void web_echo_emit_ticks(int64_t instanceId, int64_t count, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return; }
    try {
        _impl->emitTicks(count);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
    }
}

int64_t web_echo_get_counter(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return 0; }
    try {
        return _impl->get_counter();
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    }
}

void web_echo_set_counter(int64_t instanceId, int64_t value, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call web_echo_register_factory() or web_echo_register_impl()."); return; }
    try {
        _impl->set_counter(value);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
    }
}

void web_echo_register_ticks_stream(int64_t instanceId, int64_t dart_port) {
    g_ports_ticks.add(dart_port);
}
void web_echo_release_ticks_stream(int64_t dart_port) {
    g_ports_ticks.remove(dart_port);
}

} // extern "C"
