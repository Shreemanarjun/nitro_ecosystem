// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.6
// Generated from: benchmark_cpp.native.dart
// spec-sha256: 1bdf4c62b3c9282c1d56b1a944792c9a7000941ca86edb2218e541fd215d7e7a
// NativeImpl: cpp — shared C++ virtual-dispatch bridge (no JNI / Swift).
#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>
#include <string>
#include <stdexcept>
#ifdef __EMSCRIPTEN__
#include <emscripten/emscripten.h>
#include "nitro_wasm_compat.h"
#else
#include "dart_api_dl.h"
#endif
#include "benchmark_cpp.bridge.g.h"
#ifndef __EMSCRIPTEN__
#include "nitro_completion_batch.h"
static NitroCompletionBatch g_nitro_batch_benchmark_cpp;
extern "C" {
NITRO_EXPORT bool benchmark_cpp_nitro_post(int64_t port, struct _Dart_CObject* obj) { return g_nitro_batch_benchmark_cpp.post(port, obj); }
NITRO_EXPORT int64_t benchmark_cpp_nitro_bind(int64_t batchPort) { return g_nitro_batch_benchmark_cpp.bind(batchPort); }
NITRO_EXPORT void benchmark_cpp_nitro_ack(int64_t batchPort) { g_nitro_batch_benchmark_cpp.ack(batchPort); }
}
#include "nitro_worker_pool.h"
static NitroWorkerPool g_nitro_pool_benchmark_cpp;
[[maybe_unused]] static void _nitro_post_null(int64_t port) { Dart_CObject o; o.type = Dart_CObject_kNull; Dart_PostCObject_DL(port, &o); }
[[maybe_unused]] static void _nitro_post_i64(int64_t port, int64_t v) { Dart_CObject o; o.type = Dart_CObject_kInt64; o.value.as_int64 = v; Dart_PostCObject_DL(port, &o); }
[[maybe_unused]] static void _nitro_post_f64(int64_t port, double v) { Dart_CObject o; o.type = Dart_CObject_kDouble; o.value.as_double = v; Dart_PostCObject_DL(port, &o); }
[[maybe_unused]] static void _nitro_post_bool(int64_t port, int8_t v) { Dart_CObject o; o.type = Dart_CObject_kBool; o.value.as_bool = v != 0; Dart_PostCObject_DL(port, &o); }
[[maybe_unused]] static void _nitro_post_ptr(int64_t port, const void* p) { _nitro_post_i64(port, (int64_t)(intptr_t)p); }
// Owned C string (strdup'd by the async sync export): posted as kString, then freed.
[[maybe_unused]] static void _nitro_post_str_owned(int64_t port, char* s) { if (!s) { _nitro_post_null(port); return; } Dart_CObject o; o.type = Dart_CObject_kString; o.value.as_string = s; Dart_PostCObject_DL(port, &o); free(s); }
// [int32 len][payload] blob → owned copy (empty for null).
[[maybe_unused]] static std::vector<uint8_t> _nitro_copy_framed(const void* p) { if (!p) return {}; int32_t n = 0; memcpy(&n, p, 4); const uint8_t* b = (const uint8_t*)p; return std::vector<uint8_t>(b, b + 4 + (n < 0 ? 0 : n)); }
[[maybe_unused]] static std::vector<uint8_t> _nitro_copy_bytes(const void* p, size_t n) { if (!p) return {}; const uint8_t* b = (const uint8_t*)p; return std::vector<uint8_t>(b, b + n); }
// Moves the thread-local error of the worker into the per-call slot Dart reads.
[[maybe_unused]] static void _nitro_move_err(NitroError* dst, NitroError* src) { if (!dst || !src) return; dst->hasError = 1; dst->name = src->name; dst->message = src->message; dst->code = src->code; dst->stackTrace = src->stackTrace; src->hasError = 0; src->name = src->message = src->code = src->stackTrace = nullptr; }
#endif

#include "benchmark_cpp.native.g.h"

extern "C" {
NITRO_EXPORT void benchmark_cpp_make_handle_release(void* handle) {
    if (handle) { free(handle); }
}
}

extern "C" {
NITRO_EXPORT uint32_t benchmark_cpp_nitro_abi_version(void) {
    return 1;
}
NITRO_EXPORT const char* benchmark_cpp_nitro_bridge_checksum(void) {
    return "49c6b1a3d983456f";
}
NITRO_EXPORT intptr_t benchmark_cpp_init_dart_api_dl(void* data) {
    return Dart_InitializeApiDL(data);
}
#ifdef __EMSCRIPTEN__
// Web replacement for Dart_PostCObject: Dart registers a function-table
// callback here at module load (see nitro_wasm_compat.h for the envelope).
NITRO_EXPORT __attribute__((weak)) void benchmark_cpp_nitro_set_post_fn(NitroPostFn fn) {
    g_nitro_post_fn = fn;
}
// Hot restart rebuilds the module but keeps globalThis, so JS helpers a
// previous instance parked there still close over its dead heap: calls
// land out of bounds and posts reach the old instance. Returns 1 the
// first time THIS instance asks, so plugin bootstraps rebuild their
// globals exactly once per instance. JS-callable from any EM_JS body.
EM_JS(int, nitro_web_instance_changed, (), {
    var reg = globalThis.__nitroInstances || (globalThis.__nitroInstances = {});
    if (reg["benchmark_cpp"] === wasmExports) return 0;
    reg["benchmark_cpp"] = wasmExports;
    return 1;
});
EM_JS(void, nitro_web_own_globals, (const char* key), {
    var o = globalThis[UTF8ToString(key)];
    if (o) o.__nitroExports = wasmExports;
});
#endif
}

static thread_local NitroError g_nitro_error = { 0, nullptr, nullptr, nullptr, nullptr };
alignas(8) static thread_local uint8_t _g_opt_ret[16];
static thread_local std::string _g_str_ret;
// Deep copies of returned structs: Dart frees every pointer field.
[[maybe_unused]] static BenchmarkPoint _nitro_clone_BenchmarkPoint(const BenchmarkPoint& _s);
[[maybe_unused]] static BenchmarkBox _nitro_clone_BenchmarkBox(const BenchmarkBox& _s);
[[maybe_unused]] static BenchmarkPoint _nitro_clone_BenchmarkPoint(const BenchmarkPoint& _s) {
    BenchmarkPoint _c = _s;
    return _c;
}
[[maybe_unused]] static BenchmarkBox _nitro_clone_BenchmarkBox(const BenchmarkBox& _s) {
    BenchmarkBox _c = _s;
    return _c;
}


extern "C" {
NitroError* benchmark_cpp_get_error() { return &g_nitro_error; }
void benchmark_cpp_clear_error() {
    if (!g_nitro_error.hasError) { return; }
    g_nitro_error.hasError = 0;
    if (g_nitro_error.name)       { free((void*)g_nitro_error.name);       g_nitro_error.name       = nullptr; }
    if (g_nitro_error.message)    { free((void*)g_nitro_error.message);    g_nitro_error.message    = nullptr; }
    if (g_nitro_error.code)       { free((void*)g_nitro_error.code);       g_nitro_error.code       = nullptr; }
    if (g_nitro_error.stackTrace) { free((void*)g_nitro_error.stackTrace); g_nitro_error.stackTrace = nullptr; }
}
static void nitro_report_error(const char* name, const char* message, const char* code, const char* stack) {
    benchmark_cpp_clear_error();
    g_nitro_error.hasError = 1;
    g_nitro_error.name       = name    ? strdup(name)    : strdup("NativeException");
    g_nitro_error.message    = message ? strdup(message) : strdup("An unknown C++ exception occurred.");
    g_nitro_error.code       = code    ? strdup(code)    : nullptr;
    g_nitro_error.stackTrace = stack   ? strdup(stack)   : nullptr;
}
}

#include <atomic>
#include <memory>
#include <mutex>
#include <unordered_map>

using HybridBenchmarkCppFactory = std::function<std::shared_ptr<HybridBenchmarkCpp>(const std::string&)>;
static std::unordered_map<int64_t, std::shared_ptr<HybridBenchmarkCpp>>& _g_instances() {
    static std::unordered_map<int64_t, std::shared_ptr<HybridBenchmarkCpp>> m;
    return m;
}
static std::mutex& _g_instances_mtx() {
    static std::mutex m;
    return m;
}
static std::atomic<int64_t>& _g_next_instance_id() {
    static std::atomic<int64_t> id{1};
    return id;
}
static HybridBenchmarkCppFactory& _g_factory() {
    static HybridBenchmarkCppFactory f;
    return f;
}

static constexpr int _kNitroCacheWays = 8;
struct alignas(64) _NitroInstSlot {
    std::atomic<int64_t> id{-1};
    std::atomic<HybridBenchmarkCpp*> ptr{nullptr};
};
static _NitroInstSlot _g_inst_cache[_kNitroCacheWays];
static HybridBenchmarkCpp* _nitro_get_instance(int64_t id) {
    _NitroInstSlot& _slot = _g_inst_cache[(uint64_t)id & (_kNitroCacheWays - 1)];
    if (_slot.id.load(std::memory_order_acquire) == id) {
        return _slot.ptr.load(std::memory_order_relaxed);
    }
    std::lock_guard<std::mutex> _lk(_g_instances_mtx());
    auto it = _g_instances().find(id);
    HybridBenchmarkCpp* p = (it != _g_instances().end()) ? it->second.get() : nullptr;
    if (p) {
        _slot.ptr.store(p, std::memory_order_relaxed);
        _slot.id.store(id, std::memory_order_release);
    }
    return p;
}
static void _nitro_invalidate_cache() {
    for (int _i = 0; _i < _kNitroCacheWays; ++_i) {
        _g_inst_cache[_i].id.store(-1, std::memory_order_release);
        _g_inst_cache[_i].ptr.store(nullptr, std::memory_order_relaxed);
    }
}

// Defined with the stream registries below (which may be emitted after
// this block): drops every subscriber belonging to one instance.
void _nitro_release_instance_streams(const void* impl);

extern "C" {
void benchmark_cpp_register_factory(void* fn) {
    _g_factory() = *static_cast<HybridBenchmarkCppFactory*>(fn);
}
void benchmark_cpp_register_impl(HybridBenchmarkCpp* impl) {
    std::lock_guard<std::mutex> _lk(_g_instances_mtx());
    if (impl) _g_instances()[0] = std::shared_ptr<HybridBenchmarkCpp>(impl, [](HybridBenchmarkCpp*){});
    else _g_instances().erase(0);
    _nitro_invalidate_cache();
}
HybridBenchmarkCpp* benchmark_cpp_get_impl() {
    return _nitro_get_instance(0);
}
NITRO_EXPORT int64_t benchmark_cpp_create_instance(const char* key) {
    if (_g_factory()) {
        auto inst = _g_factory()(key ? key : "");
        if (!inst) return -1;
        std::lock_guard<std::mutex> _lk(_g_instances_mtx());
        int64_t id = ++_g_next_instance_id();
        _g_instances()[id] = std::move(inst);
        return id;
    }
      // Legacy: no factory registered — return 0 (single global impl slot)
    return 0;
}
NITRO_EXPORT void benchmark_cpp_destroy_instance(int64_t instanceId) {
    // Slot 0 belongs to register_impl (the legacy single impl and the
    // stream-emitter target), never to a Dart instance. Without this a
    // plugin that registers no factory hands every instance id 0, and the
    // first dispose() erases the slot for all of them.
    if (instanceId == 0) { return; }
    std::lock_guard<std::mutex> _lk(_g_instances_mtx());
    auto _it = _g_instances().find(instanceId);
    if (_it != _g_instances().end()) {
        _nitro_release_instance_streams(_it->second.get());
    }
    _g_instances().erase(instanceId);
    _nitro_invalidate_cache();
}
NITRO_EXPORT void benchmark_cpp_nitro_free(void* ptr) { if (ptr) { free(ptr); } }
NITRO_EXPORT void* benchmark_cpp_nitro_alloc(size_t size) { return malloc(size); }
}

#include <algorithm>
#include <mutex>
#include <unordered_map>
#include <vector>

// One registry per stream, PARTITIONED BY INSTANCE: a stream belongs to
// the Hybrid<Class> that emits it, so a subscriber on one instance never
// receives another instance's events (the ports used to be global, so
// every subscriber saw every instance). Every concurrent Dart subscriber
// registers its own ReceivePort. Guarded by a mutex — register/release
// run on the Dart isolate thread while emits may come from any thread.
struct _NitroStreamPorts {
    std::mutex mtx;
    std::unordered_map<const void*, std::vector<int64_t>> byInstance;
    void add(const void* inst, int64_t p) {
        std::lock_guard<std::mutex> l(mtx); byInstance[inst].push_back(p);
    }
    // Release carries only the port: Dart cancels a subscription without
    // naming the instance, so drop it wherever it is registered.
    void remove(int64_t p) {
        std::lock_guard<std::mutex> l(mtx);
        for (auto& kv : byInstance) {
            auto& v = kv.second;
            v.erase(std::remove(v.begin(), v.end(), p), v.end());
        }
    }
    void dropInstance(const void* inst) {
        std::lock_guard<std::mutex> l(mtx); byInstance.erase(inst);
    }
    std::vector<int64_t> snapshot(const void* inst) {
        std::lock_guard<std::mutex> l(mtx);
        auto it = byInstance.find(inst);
        return it == byInstance.end() ? std::vector<int64_t>() : it->second;
    }
};
static _NitroStreamPorts g_ports_pointBurstPerItem;
static _NitroStreamPorts g_ports_pointBurstBatched;
static _NitroStreamPorts g_ports_intBurstPerItem;
static _NitroStreamPorts g_ports_intBurstBatched;
static _NitroStreamPorts g_ports_dataStream;
static _NitroStreamPorts g_ports_boxStream;

// Called by destroy_instance so a disposed instance leaves no subscribers.
void _nitro_release_instance_streams(const void* impl) {
    g_ports_pointBurstPerItem.dropInstance(impl);
    g_ports_pointBurstBatched.dropInstance(impl);
    g_ports_intBurstPerItem.dropInstance(impl);
    g_ports_intBurstBatched.dropInstance(impl);
    g_ports_dataStream.dropInstance(impl);
    g_ports_boxStream.dropInstance(impl);
}

void HybridBenchmarkCpp::emit_pointBurstPerItem(BenchmarkPoint item) {
    auto _ports = g_ports_pointBurstPerItem.snapshot(this);
    if (_ports.empty()) { return; }
    Dart_CObject obj;
    for (int64_t _port : _ports) {
        BenchmarkPoint* st_ptr = (BenchmarkPoint*)malloc(sizeof(BenchmarkPoint));
        if (!st_ptr) { break; }
        *st_ptr = item;
        obj.type = Dart_CObject_kInt64;
        obj.value.as_int64 = (intptr_t)st_ptr;
        if (!Dart_PostCObject_DL(_port, &obj)) {
            free(st_ptr);
            g_ports_pointBurstPerItem.remove(_port);
        }
    }
}

void HybridBenchmarkCpp::emit_pointBurstBatched(BenchmarkPoint item) {
    auto _ports = g_ports_pointBurstBatched.snapshot(this);
    if (_ports.empty()) { return; }
    Dart_CObject obj;
    for (int64_t _port : _ports) {
        BenchmarkPoint* st_ptr = (BenchmarkPoint*)malloc(sizeof(BenchmarkPoint));
        if (!st_ptr) { break; }
        *st_ptr = item;
        obj.type = Dart_CObject_kInt64;
        obj.value.as_int64 = (intptr_t)st_ptr;
        if (!Dart_PostCObject_DL(_port, &obj)) {
            free(st_ptr);
            g_ports_pointBurstBatched.remove(_port);
        }
    }
}

void HybridBenchmarkCpp::emit_intBurstPerItem(int64_t item) {
    auto _ports = g_ports_intBurstPerItem.snapshot(this);
    if (_ports.empty()) { return; }
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = item;
    for (int64_t _port : _ports) {
        if (!Dart_PostCObject_DL(_port, &obj)) { g_ports_intBurstPerItem.remove(_port); }
    }
}

void HybridBenchmarkCpp::emit_intBurstBatched(int64_t item) {
    auto _ports = g_ports_intBurstBatched.snapshot(this);
    if (_ports.empty()) { return; }
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = item;
    for (int64_t _port : _ports) {
        if (!Dart_PostCObject_DL(_port, &obj)) { g_ports_intBurstBatched.remove(_port); }
    }
}

void HybridBenchmarkCpp::emit_dataStream(BenchmarkPoint item) {
    auto _ports = g_ports_dataStream.snapshot(this);
    if (_ports.empty()) { return; }
    Dart_CObject obj;
    for (int64_t _port : _ports) {
        BenchmarkPoint* st_ptr = (BenchmarkPoint*)malloc(sizeof(BenchmarkPoint));
        if (!st_ptr) { break; }
        *st_ptr = item;
        obj.type = Dart_CObject_kInt64;
        obj.value.as_int64 = (intptr_t)st_ptr;
        if (!Dart_PostCObject_DL(_port, &obj)) {
            free(st_ptr);
            g_ports_dataStream.remove(_port);
        }
    }
}

void HybridBenchmarkCpp::emit_boxStream(BenchmarkBox item) {
    auto _ports = g_ports_boxStream.snapshot(this);
    if (_ports.empty()) { return; }
    Dart_CObject obj;
    for (int64_t _port : _ports) {
        BenchmarkBox* st_ptr = (BenchmarkBox*)malloc(sizeof(BenchmarkBox));
        if (!st_ptr) { break; }
        *st_ptr = item;
        obj.type = Dart_CObject_kInt64;
        obj.value.as_int64 = (intptr_t)st_ptr;
        if (!Dart_PostCObject_DL(_port, &obj)) {
            free(st_ptr);
            g_ports_boxStream.remove(_port);
        }
    }
}

// S8 helper — writes error info to the out-param slot.
static void _nitro_out_err(NitroError* e, const char* name, const char* msg) {
    if (!e) return;
    e->hasError = 1;
    e->name       = name ? strdup(name) : nullptr;
    e->message    = msg  ? strdup(msg)  : nullptr;
    e->code       = nullptr;
    e->stackTrace = nullptr;
}

extern "C" {

double benchmark_cpp_add(int64_t instanceId, double a, double b, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0.0; }
    try {
        return _impl->add(a, b);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0.0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0.0;
    }
}

double benchmark_cpp_add_fast(int64_t instanceId, double a, double b, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0.0; }
    try {
        return _impl->addFast(a, b);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0.0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0.0;
    }
}

void* benchmark_cpp_make_handle(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return nullptr; }
    try {
        return _impl->makeHandle();
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

int64_t benchmark_cpp_touch_handle(int64_t instanceId, void* handle, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->touchHandle(handle);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

int64_t benchmark_cpp_touch_handle_fast(int64_t instanceId, void* handle, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->touchHandleFast(handle);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

const char* benchmark_cpp_get_greeting(int64_t instanceId, const char* name, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return nullptr; }
    try {
        std::string _res = _impl->getGreeting(std::string(name));
        _g_str_ret = _res; return const_cast<char*>(_g_str_ret.c_str());
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

int64_t benchmark_cpp_hash_buffer(int64_t instanceId, uint8_t* data, size_t data_length, int64_t rounds, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->hashBuffer(data, static_cast<size_t>(data_length), rounds);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

int64_t benchmark_cpp_sieve_primes(int64_t instanceId, int64_t limit, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->sievePrimes(limit);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

void* benchmark_cpp_scale_point(int64_t instanceId, void* point, double factor, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return nullptr; }
    try {
        BenchmarkPoint _res = _impl->scalePoint(*static_cast<const BenchmarkPoint*>(point), factor);
        static thread_local BenchmarkPoint _g_ret_st;
        BenchmarkPoint* _ptr = &_g_ret_st;
        *_ptr = _nitro_clone_BenchmarkPoint(_res);
        return _ptr;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void* benchmark_cpp_compute_stats(int64_t instanceId, int64_t iterations) {
    NitroError* _nitro_err = nullptr; // async: errors use TLS not out-param
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _res = _impl->computeStats(iterations);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void benchmark_cpp_compute_stats_native(int64_t instanceId, int64_t iterations, NitroError* _nitro_err, int64_t dart_port) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) {
        _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered.");
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
        return;
    }
    try {
        _impl->computeStatsNative(iterations, _nitro_err, dart_port);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
    }
}

uint8_t* benchmark_cpp_echo_int_map(int64_t instanceId, uint8_t* map, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _buf_map = { (const uint8_t*)map + 4, (size_t)*(int32_t*)map };
        NitroCppBuffer _res = _impl->echoIntMap(_buf_map);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

void* benchmark_cpp_echo_stats_list(int64_t instanceId, void* stats, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return nullptr; }
    try {
        NitroCppBuffer _buf_stats = { (const uint8_t*)stats + 4, (size_t)*(int32_t*)stats };
        NitroCppBuffer _res = _impl->echoStatsList(_buf_stats);
        return (uint8_t*)_res.data;
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return nullptr;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return nullptr;
    }
}

int64_t benchmark_cpp_async_echo(int64_t instanceId, int64_t value) {
    NitroError* _nitro_err = nullptr; // async: errors use TLS not out-param
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->asyncEcho(value);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

void benchmark_cpp_native_async_echo(int64_t instanceId, int64_t value, NitroError* _nitro_err, int64_t dart_port) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) {
        _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered.");
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
        return;
    }
    try {
        _impl->nativeAsyncEcho(value, _nitro_err, dart_port);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
    }
}

int64_t benchmark_cpp_native_async_echo_inline(int64_t instanceId, int64_t value, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->nativeAsyncEchoInline(value);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

int64_t benchmark_cpp_native_async_echo_inline_or(int64_t instanceId, int64_t value, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->nativeAsyncEchoInlineOr(value);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

void benchmark_cpp_native_async_echo_from_thread(int64_t instanceId, int64_t value, NitroError* _nitro_err, int64_t dart_port) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) {
        _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered.");
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
        return;
    }
    try {
        _impl->nativeAsyncEchoFromThread(value, _nitro_err, dart_port);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        Dart_CObject _err = { Dart_CObject_kNull };
        Dart_PostCObject_DL(dart_port, &_err);
    }
}

void benchmark_cpp_submit_coalesced(int64_t instanceId, int64_t callId, int64_t value, int64_t dartPort, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return; }
    try {
        _impl->submitCoalesced(callId, value, dartPort);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
    }
}

void benchmark_cpp_reset_coalesce_stats(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return; }
    try {
        _impl->resetCoalesceStats();
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
    }
}

int64_t benchmark_cpp_coalesce_flushes(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->coalesceFlushes();
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

int64_t benchmark_cpp_coalesce_items(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->coalesceItems();
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

void benchmark_cpp_burst_points(int64_t instanceId, int64_t count, int8_t batched, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return; }
    try {
        _impl->burstPoints(count, batched);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
    }
}

void benchmark_cpp_burst_ints(int64_t instanceId, int64_t count, int8_t batched, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return; }
    try {
        _impl->burstInts(count, batched);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
    }
}

int64_t benchmark_cpp_send_large_buffer_fast(int64_t instanceId, uint8_t* buffer, size_t buffer_length, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->sendLargeBufferFast(buffer, static_cast<size_t>(buffer_length));
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

int64_t benchmark_cpp_send_large_buffer_noop(int64_t instanceId, uint8_t* buffer, size_t buffer_length, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->sendLargeBufferNoop(buffer, static_cast<size_t>(buffer_length));
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

int64_t benchmark_cpp_send_large_buffer_noop_fast(int64_t instanceId, uint8_t* buffer, size_t buffer_length, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->sendLargeBufferNoopFast(buffer, static_cast<size_t>(buffer_length));
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

int64_t benchmark_cpp_send_large_buffer_unsafe(int64_t instanceId, void* ptr, int64_t length, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    auto _impl = _nitro_get_instance(instanceId);
    if (!_impl) { _nitro_out_err(_nitro_err, "NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_factory() or benchmark_cpp_register_impl()."); return 0; }
    try {
        return _impl->sendLargeBufferUnsafe(static_cast<uint8_t*>(ptr), length);
    } catch (const std::exception& e) {
        _nitro_out_err(_nitro_err, "CppException", e.what());
        return 0;
    } catch (...) {
        _nitro_out_err(_nitro_err, "CppException", "Unknown C++ exception");
        return 0;
    }
}

#ifndef __EMSCRIPTEN__
void benchmark_cpp_release_BenchmarkPoint(void* ptr);
void benchmark_cpp_release_BenchmarkBox(void* ptr);
NITRO_EXPORT void benchmark_cpp_compute_stats_dispatch(int64_t instanceId, int64_t iterations, NitroError* _nitro_err, int64_t dart_port) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
    g_nitro_pool_benchmark_cpp.enqueue([=]() mutable {
        benchmark_cpp_clear_error();
        void* _r = benchmark_cpp_compute_stats(instanceId, iterations);
        NitroError* _e = benchmark_cpp_get_error();
        if (_e->hasError) { _nitro_move_err(_nitro_err, _e); _nitro_post_null(dart_port); return; }
        _nitro_post_ptr(dart_port, (const void*)_r);
    });
}

NITRO_EXPORT void benchmark_cpp_async_echo_dispatch(int64_t instanceId, int64_t value, NitroError* _nitro_err, int64_t dart_port) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
    g_nitro_pool_benchmark_cpp.enqueue([=]() mutable {
        benchmark_cpp_clear_error();
        int64_t _r = benchmark_cpp_async_echo(instanceId, value);
        NitroError* _e = benchmark_cpp_get_error();
        if (_e->hasError) { _nitro_move_err(_nitro_err, _e); _nitro_post_null(dart_port); return; }
        _nitro_post_i64(dart_port, _r);
    });
}

#endif
void benchmark_cpp_register_point_burst_per_item_stream(int64_t instanceId, int64_t dart_port) {
    g_ports_pointBurstPerItem.add(_nitro_get_instance(instanceId), dart_port);
}
void benchmark_cpp_release_point_burst_per_item_stream(int64_t dart_port) {
    g_ports_pointBurstPerItem.remove(dart_port);
}

void benchmark_cpp_register_point_burst_batched_stream(int64_t instanceId, int64_t dart_port) {
#ifndef __EMSCRIPTEN__
    g_nitro_batch_benchmark_cpp.coalesce(dart_port);
#endif
    g_ports_pointBurstBatched.add(_nitro_get_instance(instanceId), dart_port);
}
void benchmark_cpp_release_point_burst_batched_stream(int64_t dart_port) {
    g_ports_pointBurstBatched.remove(dart_port);
#ifndef __EMSCRIPTEN__
    g_nitro_batch_benchmark_cpp.uncoalesce(dart_port);
#endif
}

void benchmark_cpp_register_int_burst_per_item_stream(int64_t instanceId, int64_t dart_port) {
    g_ports_intBurstPerItem.add(_nitro_get_instance(instanceId), dart_port);
}
void benchmark_cpp_release_int_burst_per_item_stream(int64_t dart_port) {
    g_ports_intBurstPerItem.remove(dart_port);
}

void benchmark_cpp_register_int_burst_batched_stream(int64_t instanceId, int64_t dart_port) {
#ifndef __EMSCRIPTEN__
    g_nitro_batch_benchmark_cpp.coalesce(dart_port);
#endif
    g_ports_intBurstBatched.add(_nitro_get_instance(instanceId), dart_port);
}
void benchmark_cpp_release_int_burst_batched_stream(int64_t dart_port) {
    g_ports_intBurstBatched.remove(dart_port);
#ifndef __EMSCRIPTEN__
    g_nitro_batch_benchmark_cpp.uncoalesce(dart_port);
#endif
}

void benchmark_cpp_register_data_stream_stream(int64_t instanceId, int64_t dart_port) {
    g_ports_dataStream.add(_nitro_get_instance(instanceId), dart_port);
}
void benchmark_cpp_release_data_stream_stream(int64_t dart_port) {
    g_ports_dataStream.remove(dart_port);
}

void benchmark_cpp_register_box_stream_stream(int64_t instanceId, int64_t dart_port) {
    g_ports_boxStream.add(_nitro_get_instance(instanceId), dart_port);
}
void benchmark_cpp_release_box_stream_stream(int64_t dart_port) {
    g_ports_boxStream.remove(dart_port);
}

// Frees a malloc'd [BenchmarkPoint] wrapper allocated by the stream emitter.
// Called automatically by NativeFinalizer when the Dart proxy is GC'd.
void benchmark_cpp_release_BenchmarkPoint(void* ptr) {
    if (!ptr) { return; }
    free(ptr);
}

// Frees a malloc'd [BenchmarkBox] wrapper allocated by the stream emitter.
// Called automatically by NativeFinalizer when the Dart proxy is GC'd.
void benchmark_cpp_release_BenchmarkBox(void* ptr) {
    if (!ptr) { return; }
    free(ptr);
}

} // extern "C"
