// Generated by Nitrogen Modules. Do not edit.
// Generated from: benchmark_cpp.native.dart
// NativeImpl: cpp — direct C++ virtual-dispatch bridge (no JNI / Swift).
#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>
#include <string>
#include <stdexcept>
#include "dart_api_dl.h"
#include "benchmark_cpp.bridge.g.h"
#include "benchmark_cpp.native.g.h"

extern "C" {
intptr_t benchmark_cpp_init_dart_api_dl(void* data) {
    return Dart_InitializeApiDL(data);
}
}

static thread_local NitroError g_nitro_error = { 0, nullptr, nullptr, nullptr, nullptr };

extern "C" {
NitroError* benchmark_cpp_get_error() { return &g_nitro_error; }
void benchmark_cpp_clear_error() {
    if (!g_nitro_error.hasError) return;
    g_nitro_error.hasError = 0;
    if (g_nitro_error.name)       { free((void*)g_nitro_error.name);       g_nitro_error.name       = nullptr; }
    if (g_nitro_error.message)    { free((void*)g_nitro_error.message);    g_nitro_error.message    = nullptr; }
    if (g_nitro_error.code)       { free((void*)g_nitro_error.code);       g_nitro_error.code       = nullptr; }
    if (g_nitro_error.stackTrace) { free((void*)g_nitro_error.stackTrace); g_nitro_error.stackTrace = nullptr; }
}
static void nitro_report_error(const char* name, const char* message, const char* code, const char* stack) {
    benchmark_cpp_clear_error();
    g_nitro_error.hasError = 1;
    g_nitro_error.name       = name    ? strdup(name)    : strdup("NativeException");
    g_nitro_error.message    = message ? strdup(message) : strdup("An unknown C++ exception occurred.");
    g_nitro_error.code       = code    ? strdup(code)    : nullptr;
    g_nitro_error.stackTrace = stack   ? strdup(stack)   : nullptr;
}
}

// g_impl is written once during DSO load (see __attribute__((constructor)))
// and is read-only during concurrent bridge calls — no std::atomic needed.
static HybridBenchmarkCpp* g_impl = nullptr;

void benchmark_cpp_register_impl(HybridBenchmarkCpp* impl) { g_impl = impl; }
HybridBenchmarkCpp* benchmark_cpp_get_impl() { return g_impl; }

static int64_t g_port_dataStream = 0;
static int64_t g_port_boxStream = 0;

void HybridBenchmarkCpp::emit_dataStream(BenchmarkPoint item) {
    int64_t port = g_port_dataStream;
    if (port == 0) return;
    Dart_CObject obj;
    BenchmarkPoint* st_ptr = (BenchmarkPoint*)malloc(sizeof(BenchmarkPoint));
    *st_ptr = item;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (intptr_t)st_ptr;
    Dart_PostCObject_DL(port, &obj);
}

void HybridBenchmarkCpp::emit_boxStream(BenchmarkBox item) {
    int64_t port = g_port_boxStream;
    if (port == 0) return;
    Dart_CObject obj;
    BenchmarkBox* st_ptr = (BenchmarkBox*)malloc(sizeof(BenchmarkBox));
    *st_ptr = item;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (intptr_t)st_ptr;
    Dart_PostCObject_DL(port, &obj);
}

extern "C" {

double benchmark_cpp_add(double a, double b) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return 0.0; }
    try {
        return g_impl->add(a, b);
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return 0.0;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return 0.0;
    }
}

double benchmark_cpp_add_fast(double a, double b) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return 0.0; }
    try {
        return g_impl->addFast(a, b);
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return 0.0;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return 0.0;
    }
}

const char* benchmark_cpp_get_greeting(const char* name) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return nullptr; }
    try {
        std::string _res = g_impl->getGreeting(std::string(name));
        return strdup(_res.c_str());
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return nullptr;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return nullptr;
    }
}

void* benchmark_cpp_scale_point(void* point, double factor) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return nullptr; }
    try {
        BenchmarkPoint _res = g_impl->scalePoint(*static_cast<const BenchmarkPoint*>(point), factor);
        BenchmarkPoint* _ptr = (BenchmarkPoint*)malloc(sizeof(BenchmarkPoint));
        *_ptr = _res;
        return _ptr;
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return nullptr;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return nullptr;
    }
}

void* benchmark_cpp_compute_stats(int64_t iterations) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return nullptr; }
    try {
        NitroCppBuffer _res = g_impl->computeStats(iterations);
        return (void*)_res.data;
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return nullptr;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return nullptr;
    }
}

int64_t benchmark_cpp_send_large_buffer_fast(uint8_t* buffer, int64_t buffer_length) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return 0; }
    try {
        return g_impl->sendLargeBufferFast(buffer, static_cast<size_t>(buffer_length));
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return 0;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return 0;
    }
}

int64_t benchmark_cpp_send_large_buffer_noop(uint8_t* buffer, int64_t buffer_length) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return 0; }
    try {
        return g_impl->sendLargeBufferNoop(buffer, static_cast<size_t>(buffer_length));
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return 0;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return 0;
    }
}

int64_t benchmark_cpp_send_large_buffer_noop_fast(uint8_t* buffer, int64_t buffer_length) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return 0; }
    try {
        return g_impl->sendLargeBufferNoopFast(buffer, static_cast<size_t>(buffer_length));
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return 0;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return 0;
    }
}

int64_t benchmark_cpp_send_large_buffer_unsafe(void* ptr, int64_t length) {
    benchmark_cpp_clear_error();
    if (!g_impl) { nitro_report_error("NotInitialized", "No C++ implementation registered. Call benchmark_cpp_register_impl() first.", nullptr, nullptr); return 0; }
    try {
        return g_impl->sendLargeBufferUnsafe(ptr, length);
    } catch (const std::exception& e) {
        nitro_report_error("CppException", e.what(), nullptr, nullptr);
        return 0;
    } catch (...) {
        nitro_report_error("CppException", "Unknown C++ exception", nullptr, nullptr);
        return 0;
    }
}

void benchmark_cpp_register_data_stream_stream(int64_t dart_port) {
    g_port_dataStream = dart_port;
}
void benchmark_cpp_release_data_stream_stream(int64_t dart_port) {
    if (g_port_dataStream == dart_port) g_port_dataStream = 0;
}

void benchmark_cpp_register_box_stream_stream(int64_t dart_port) {
    g_port_boxStream = dart_port;
}
void benchmark_cpp_release_box_stream_stream(int64_t dart_port) {
    if (g_port_boxStream == dart_port) g_port_boxStream = 0;
}

} // extern "C"
