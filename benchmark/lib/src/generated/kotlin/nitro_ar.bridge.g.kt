// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.5.5
// Generated from: nitro_ar.native.dart
package nitro.nitro_ar_module

import android.app.Activity
import android.content.Context
import androidx.annotation.Keep
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.launch
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.CoroutineStart
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.runBlocking

// --- Structs ---
@androidx.annotation.Keep
data class Vector3(val x: Double, val y: Double, val z: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): Vector3 {
            val x = buf.double
            val y = buf.double
            val z = buf.double
            return Vector3(x, y, z)
        }
        @JvmStatic fun decode(bytes: ByteArray): Vector3 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeDouble(x)
        writeDouble(y)
        writeDouble(z)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(24)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class Quaternion(val x: Double, val y: Double, val z: Double, val w: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): Quaternion {
            val x = buf.double
            val y = buf.double
            val z = buf.double
            val w = buf.double
            return Quaternion(x, y, z, w)
        }
        @JvmStatic fun decode(bytes: ByteArray): Quaternion {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeDouble(x)
        writeDouble(y)
        writeDouble(z)
        writeDouble(w)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(32)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class BoundingBox(val x: Double, val y: Double, val width: Double, val height: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): BoundingBox {
            val x = buf.double
            val y = buf.double
            val width = buf.double
            val height = buf.double
            return BoundingBox(x, y, width, height)
        }
        @JvmStatic fun decode(bytes: ByteArray): BoundingBox {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeDouble(x)
        writeDouble(y)
        writeDouble(width)
        writeDouble(height)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(32)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class PackageDimensions(val length: Double, val width: Double, val height: Double, val confidence: Double, val vector3: Vector3, val quaternion: Quaternion) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PackageDimensions {
            val length = buf.double
            val width = buf.double
            val height = buf.double
            val confidence = buf.double
            val vector3 = Vector3.decodeFrom(buf)
            val quaternion = Quaternion.decodeFrom(buf)
            return PackageDimensions(length, width, height, confidence, vector3, quaternion)
        }
        @JvmStatic fun decode(bytes: ByteArray): PackageDimensions {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeDouble(length)
        writeDouble(width)
        writeDouble(height)
        writeDouble(confidence)
        vector3.writeFieldsTo(out, buf)
        quaternion.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(96)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class RawDepthMap(val data: java.nio.ByteBuffer, val width: Long, val height: Long, val stride: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RawDepthMap {
            val data = { buf.long; java.nio.ByteBuffer.allocate(0) }()
            val width = buf.long
            val height = buf.long
            val stride = buf.long
            return RawDepthMap(data, width, height, stride)
        }
        @JvmStatic fun decode(bytes: ByteArray): RawDepthMap {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(0L)
        writeInt(width)
        writeInt(height)
        writeInt(stride)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(60)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

// --- @HybridRecord Kotlin data classes (generated by Nitrogen) ---

@androidx.annotation.Keep
data class PackageBoxes(val boxes: List<Double>) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PackageBoxes {
            val boxes = (0 until buf.int).map { buf.double }
            return PackageBoxes(boxes)
        }
        @JvmStatic fun decode(bytes: ByteArray): PackageBoxes {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PackageBoxes = PackageBoxes(
                boxes = @Suppress("UNCHECKED_CAST") (map["boxes"] as List<Double>)
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(36) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt32(boxes.size)
        boxes.forEach { e -> writeDouble(e) }
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PackageBoxes> support ---
    fun toJson(): Map<String, Any?> = mapOf("boxes" to boxes)

}

@androidx.annotation.Keep
data class LiveTrackingUpdate(val isTracking: Boolean, val centerDimensions: PackageDimensions) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): LiveTrackingUpdate {
            val isTracking = (buf.get().toInt() != 0)
            val centerDimensions = PackageDimensions.decodeFrom(buf)
            return LiveTrackingUpdate(isTracking, centerDimensions)
        }
        @JvmStatic fun decode(bytes: ByteArray): LiveTrackingUpdate {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): LiveTrackingUpdate = LiveTrackingUpdate(
                isTracking = map["isTracking"] as Boolean,
                centerDimensions = throw UnsupportedOperationException("Struct field \'centerDimensions\' cannot be reconstructed from JSON — use binary bridge instead")
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(65) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(isTracking)
        centerDimensions.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, LiveTrackingUpdate> support ---
    fun toJson(): Map<String, Any?> = mapOf("isTracking" to isTracking, "centerDimensions" to centerDimensions)

}

@androidx.annotation.Keep
data class NitroNullableInt(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableInt {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroNullableInt(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableInt {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableInt = NitroNullableInt(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableInt> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableDouble(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableDouble {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroNullableDouble(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableDouble {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableDouble = NitroNullableDouble(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableDouble> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroNullableBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableBool = NitroNullableBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptInt64(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptInt64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroOptInt64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptInt64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptInt64 = NitroOptInt64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptInt64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptFloat64(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptFloat64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroOptFloat64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptFloat64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptFloat64 = NitroOptFloat64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptFloat64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroOptBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptBool = NitroOptBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

/**
 * Contract for the [NitroAr] module.
 * Implement this in your Kotlin source code.
 * Nitro may call this implementation from any JNI thread.
 * Keep mutable state thread-safe or marshal work onto your own dispatcher.
 */
interface HybridNitroArSpec {
    val applicationContext: Context get() = NitroArJniBridge.applicationContext
    val activity: Activity? get() = NitroArJniBridge.activity

    // Optional lifecycle hooks — override only what you need.
    fun onAttached() {}
    fun onDetached() {}
    fun onActivityAttached(activity: Activity) {}
    fun onActivityDetached() {}

    // source: nitro_ar.native.dart:105
    fun add(a: Double, b: Double): Double
    // source: nitro_ar.native.dart:108
    suspend fun getGreeting(name: String): String
    // source: nitro_ar.native.dart:110
    fun isDepthSupported(): Boolean
    // source: nitro_ar.native.dart:112
    fun detectPackage(rect: BoundingBox): PackageDimensions
    // source: nitro_ar.native.dart:114
    fun getRawDepthMap(): RawDepthMap
    // source: nitro_ar.native.dart:116
    fun estimateVolume(anchor: String): Double
    // source: nitro_ar.native.dart:118
    suspend fun checkCameraPermission(): Boolean
    // source: nitro_ar.native.dart:121
    suspend fun requestCameraPermission(): Boolean
    // source: nitro_ar.native.dart:125
    suspend fun startSession(): Unit
    // source: nitro_ar.native.dart:128
    suspend fun stopSession(): Unit
    // source: nitro_ar.native.dart:131
    suspend fun pauseSession(): Unit
    // source: nitro_ar.native.dart:134
    suspend fun resumeSession(): Unit
    // source: nitro_ar.native.dart:137
    fun isTracking(): Boolean
    // source: nitro_ar.native.dart:139
    fun enableFlashlight(enable: Boolean): Unit
    // source: nitro_ar.native.dart:142
    fun setDetectionOptions(threshold: Double, rotation: Long, useMock: Boolean): Unit
    val detectedPackages: Flow<PackageBoxes>
    val liveTrackingUpdates: Flow<LiveTrackingUpdate>
}

@Keep
object NitroArJniBridge {
    private val _implementations = java.util.concurrent.ConcurrentHashMap<Long, HybridNitroArSpec>()
    private val _idCounter = java.util.concurrent.atomic.AtomicLong(0)
    private var _factory: (() -> HybridNitroArSpec)? = null
    private val _asyncExecutor = java.util.concurrent.Executors.newCachedThreadPool()

    lateinit var applicationContext: Context
        private set

    var activity: Activity? = null
        private set

    @JvmStatic external fun initialize(bridgeClass: Class<*>)

    fun registerFactory(factory: () -> HybridNitroArSpec, context: Context) {
        applicationContext = context
        _factory = factory
        if (_implementations.isEmpty()) { initialize(this::class.java) }
    }

    @JvmStatic
    fun create_instance_call(key: String): Long {
        val factory = _factory ?: throw IllegalStateException(
            "NitroAr: no factory registered. Call registerFactory() in onAttachedToEngine().")
        val id = _idCounter.getAndIncrement()
        val impl = factory()
        _implementations[id] = impl
        impl.onAttached()
        return id
    }

    @JvmStatic
    fun destroy_instance_call(instanceId: Long) {
        _implementations.remove(instanceId)?.onDetached()
    }

    fun onActivityAttached(newActivity: Activity) {
        activity = newActivity
        _implementations.values.forEach { it.onActivityAttached(newActivity) }
    }

    fun onActivityDetached() {
        activity = null
        _implementations.values.forEach { it.onActivityDetached() }
    }

    // source: nitro_ar.native.dart:105
    @JvmStatic fun add_call(instanceId: Long, a: Double, b: Double): Double {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return impl.add(a, b)
    }
    // source: nitro_ar.native.dart:108
    @JvmStatic fun getGreeting_call(instanceId: Long, name: String): String {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.getGreeting(name) }
        }).get()
    }
    // source: nitro_ar.native.dart:110
    @JvmStatic fun isDepthSupported_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return impl.isDepthSupported()
    }
    // source: nitro_ar.native.dart:112
    @JvmStatic fun detectPackage_call(instanceId: Long, rect: BoundingBox): PackageDimensions {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return impl.detectPackage(rect)
    }
    // source: nitro_ar.native.dart:114
    @JvmStatic fun getRawDepthMap_call(instanceId: Long): RawDepthMap {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return impl.getRawDepthMap()
    }
    // source: nitro_ar.native.dart:116
    @JvmStatic fun estimateVolume_call(instanceId: Long, anchor: String): Double {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return impl.estimateVolume(anchor)
    }
    // source: nitro_ar.native.dart:118
    @JvmStatic fun checkCameraPermission_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.checkCameraPermission() }
        }).get()
    }
    // source: nitro_ar.native.dart:121
    @JvmStatic fun requestCameraPermission_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.requestCameraPermission() }
        }).get()
    }
    // source: nitro_ar.native.dart:125
    @JvmStatic fun startSession_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.startSession() }
        }).get()
    }
    // source: nitro_ar.native.dart:128
    @JvmStatic fun stopSession_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.stopSession() }
        }).get()
    }
    // source: nitro_ar.native.dart:131
    @JvmStatic fun pauseSession_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.pauseSession() }
        }).get()
    }
    // source: nitro_ar.native.dart:134
    @JvmStatic fun resumeSession_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return _asyncExecutor.submit(java.util.concurrent.Callable {
            runBlocking { impl.resumeSession() }
        }).get()
    }
    // source: nitro_ar.native.dart:137
    @JvmStatic fun isTracking_call(instanceId: Long): Boolean {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        return impl.isTracking()
    }
    // source: nitro_ar.native.dart:139
    @JvmStatic fun enableFlashlight_call(instanceId: Long, enable: Boolean): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        impl.enableFlashlight(enable)
    }
    // source: nitro_ar.native.dart:142
    @JvmStatic fun setDetectionOptions_call(instanceId: Long, threshold: Double, rotation: Long, useMock: Boolean): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroAr instance $instanceId not registered")
        impl.setDetectionOptions(threshold, rotation, useMock)
    }
    private val _streamJobs = java.util.concurrent.ConcurrentHashMap<Pair<String, Long>, kotlinx.coroutines.Job>()

    @JvmStatic external fun emit_detectedPackages(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_ar_register_detected_packages_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("detectedPackages", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.detectedPackages.collect { item -> 
                if (!emit_detectedPackages(dartPort, item.encode())) {
                    _streamJobs.remove(Pair("detectedPackages", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_ar_release_detected_packages_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("detectedPackages", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_liveTrackingUpdates(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_ar_register_live_tracking_updates_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("liveTrackingUpdates", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.liveTrackingUpdates.collect { item -> 
                if (!emit_liveTrackingUpdates(dartPort, item.encode())) {
                    _streamJobs.remove(Pair("liveTrackingUpdates", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_ar_release_live_tracking_updates_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("liveTrackingUpdates", dartPort))?.cancel()
    }
}
