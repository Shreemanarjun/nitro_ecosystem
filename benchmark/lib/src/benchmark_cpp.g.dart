// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.5.9
// Generated from: benchmark_cpp.native.dart
// ignore_for_file: no_leading_underscores_for_local_identifiers, prefer_typing_uninitialized_variables, non_constant_identifier_names
part of 'benchmark_cpp.native.dart';

// --- Struct FFI representations (generated by Nitrogen) ---

@Packed(1)
final class BenchmarkPointFfi extends Struct {
  @Double()
  external double x;
  @Double()
  external double y;
}

extension BenchmarkPointFfiExt on BenchmarkPointFfi {
  BenchmarkPoint toDart() {
    return BenchmarkPoint(x: x, y: y);
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  void freeFields() {}
}

extension BenchmarkPointExt on BenchmarkPoint {
  Pointer<BenchmarkPointFfi> toNative(Arena arena) {
    final ptr = arena<BenchmarkPointFfi>();
    ptr.ref.x = x;
    ptr.ref.y = y;
    return ptr;
  }
}

@Packed(1)
final class BenchmarkBoxFfi extends Struct {
  @Int64()
  external int color;
  @Double()
  external double width;
  @Double()
  external double height;
}

extension BenchmarkBoxFfiExt on BenchmarkBoxFfi {
  BenchmarkBox toDart() {
    return BenchmarkBox(color: color, width: width, height: height);
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  void freeFields() {}
}

extension BenchmarkBoxExt on BenchmarkBox {
  Pointer<BenchmarkBoxFfi> toNative(Arena arena) {
    final ptr = arena<BenchmarkBoxFfi>();
    ptr.ref.color = color;
    ptr.ref.width = width;
    ptr.ref.height = height;
    return ptr;
  }
}

// --- Struct Native Proxies (zero-copy, lazy field access) ---

/// Zero-copy proxy for [BenchmarkPoint].
/// Extends [BenchmarkPoint] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `BenchmarkPointProxy <: BenchmarkPoint`, a `Stream<BenchmarkPointProxy>`
/// satisfies `Stream<BenchmarkPoint>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'benchmark_cpp_release_BenchmarkPoint'.
/// Ownership: this proxy owns the generated struct shell. Zero-copy
/// field buffers remain owned by the native implementation and must
/// stay valid until this proxy is released by the finalizer or
/// [toDartAndRelease].
final class BenchmarkPointProxy extends BenchmarkPoint implements Finalizable {
  final Pointer<BenchmarkPointFfi> _native;

  static NativeFinalizer? _finalizer;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
        'benchmark_cpp_release_BenchmarkPoint',
      ),
    );
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] on the struct shell after passing it here,
  /// and do not free zero-copy field buffers while the proxy may be read.
  BenchmarkPointProxy(this._native) : super(x: 0.0, y: 0.0) {
    assert(
      _finalizer != null,
      'BenchmarkPointProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.',
    );
    _finalizer!.attach(this, _native.cast(), detach: this, externalSize: 512);
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  double get x => _native.ref.x;
  @override
  double get y => _native.ref.y;

  /// Eagerly copies all fields to a plain [BenchmarkPoint] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  BenchmarkPoint toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    malloc.free(_native);
    return v;
  }
}

/// Zero-copy proxy for [BenchmarkBox].
/// Extends [BenchmarkBox] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `BenchmarkBoxProxy <: BenchmarkBox`, a `Stream<BenchmarkBoxProxy>`
/// satisfies `Stream<BenchmarkBox>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'benchmark_cpp_release_BenchmarkBox'.
/// Ownership: this proxy owns the generated struct shell. Zero-copy
/// field buffers remain owned by the native implementation and must
/// stay valid until this proxy is released by the finalizer or
/// [toDartAndRelease].
final class BenchmarkBoxProxy extends BenchmarkBox implements Finalizable {
  final Pointer<BenchmarkBoxFfi> _native;

  static NativeFinalizer? _finalizer;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
        'benchmark_cpp_release_BenchmarkBox',
      ),
    );
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] on the struct shell after passing it here,
  /// and do not free zero-copy field buffers while the proxy may be read.
  BenchmarkBoxProxy(this._native) : super(color: 0, width: 0.0, height: 0.0) {
    assert(
      _finalizer != null,
      'BenchmarkBoxProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.',
    );
    _finalizer!.attach(this, _native.cast(), detach: this, externalSize: 512);
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  int get color => _native.ref.color;
  @override
  double get width => _native.ref.width;
  @override
  double get height => _native.ref.height;

  /// Eagerly copies all fields to a plain [BenchmarkBox] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  BenchmarkBox toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    malloc.free(_native);
    return v;
  }
}

// --- @HybridRecord binary extensions (generated by Nitrogen) ---

extension BenchmarkStatsRecordExt on BenchmarkStats {
  static BenchmarkStats fromNative(Pointer<Uint8> ptr) =>
      fromReader(RecordReader.fromNative(ptr));

  static BenchmarkStats fromReader(RecordReader r) => BenchmarkStats(
    count: r.readInt(),
    meanUs: r.readDouble(),
    minUs: r.readDouble(),
    maxUs: r.readDouble(),
  );

  void writeFields(RecordWriter writer) {
    writer.writeInt(count);
    writer.writeDouble(meanUs);
    writer.writeDouble(minUs);
    writer.writeDouble(maxUs);
  }

  Pointer<Uint8> toNative(Allocator alloc) {
    final writer = RecordWriter();
    writeFields(writer);
    return writer.toNative(alloc);
  }
}

class _BenchmarkCppImpl extends BenchmarkCpp {
  final DynamicLibrary _dylib;
  static final _instances = <String, _BenchmarkCppImpl>{};
  final String _instanceKey;
  late final int _instanceId;
  final Pointer<NitroErrorFfi> _nitroErr = calloc<NitroErrorFfi>();

  static DynamicLibrary _loadSupportedLibrary() {
    assert(
      !const bool.fromEnvironment('dart.library.js_interop'),
      'benchmark_cpp: dart:ffi is unavailable on web. Instantiate the web bridge via createBenchmarkCppWebInstance() from the generated *.web.bridge.g.dart file instead.',
    );
    return NitroRuntime.loadLibForTargets(
      'benchmark_cpp',
      ios: true,
      android: true,
      macos: true,
      windows: true,
      linux: true,
      web: true,
    );
  }

  factory _BenchmarkCppImpl([String key = 'default']) {
    return _instances.putIfAbsent(key, () => _BenchmarkCppImpl._init(key));
  }

  _BenchmarkCppImpl._init(this._instanceKey)
    : _dylib = _loadSupportedLibrary() {
    final initSw = Stopwatch()..start();
    assert(
      sizeOf<IntPtr>() >= 4,
      'benchmark_cpp: unsupported pointer width ${sizeOf<IntPtr>()}B',
    );
    final initFunc = _dylib
        .lookupFunction<
          IntPtr Function(Pointer<Void>),
          int Function(Pointer<Void>)
        >('benchmark_cpp_init_dart_api_dl');
    final initCode = initFunc(NativeApi.initializeApiDLData);
    if (initCode != 0) {
      throw StateError(
        'benchmark_cpp: Dart API DL initialization failed with code $initCode.',
      );
    }
    if (!_dylib.providesSymbol('benchmark_cpp_nitro_abi_version')) {
      throw StateError(
        'benchmark_cpp: missing benchmark_cpp_nitro_abi_version — plugin must be rebuilt against the current Nitro version.',
      );
    }
    NitroRuntime.checkAbiVersion(
      'benchmark_cpp',
      () => _dylib.lookupFunction<Uint32 Function(), int Function()>(
        'benchmark_cpp_nitro_abi_version',
      )(),
    );
    if (!_dylib.providesSymbol('benchmark_cpp_nitro_bridge_checksum')) {
      throw StateError(
        'benchmark_cpp: missing benchmark_cpp_nitro_bridge_checksum — plugin must be rebuilt against the current Nitro version.',
      );
    }
    NitroRuntime.checkLinkChecksum(
      'benchmark_cpp',
      '94489efbfa327f2e',
      () => _dylib
          .lookupFunction<Pointer<Utf8> Function(), Pointer<Utf8> Function()>(
            'benchmark_cpp_nitro_bridge_checksum',
          )()
          .toDartString(),
    );
    BenchmarkPointProxy._init(_dylib);
    BenchmarkBoxProxy._init(_dylib);
    final _keyPtr = _instanceKey.toNativeUtf8(allocator: calloc);
    try {
      _instanceId = _createInstancePtr(_keyPtr);
      if (_instanceId < 0) {
        throw StateError(
          'benchmark_cpp: failed to create native instance for key "$_instanceKey".',
        );
      }
    } finally {
      calloc.free(_keyPtr);
    }
    NitroInstanceRegistry.register(_instanceId, this);
    initSw.stop();
    NitroRuntime.logLifecycle(
      'init(benchmark_cpp)',
      'initialized in ${initSw.elapsedMicroseconds} µs (instanceId=$_instanceId)',
    );
  }

  late final int Function(Pointer<Utf8>) _createInstancePtr = _dylib
      .lookupFunction<
        Int64 Function(Pointer<Utf8>),
        int Function(Pointer<Utf8>)
      >('benchmark_cpp_create_instance');
  late final void Function(int) _destroyInstancePtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'benchmark_cpp_destroy_instance',
      );
  late final double Function(int, double, double, Pointer<NitroErrorFfi>)
  _addPtr = _dylib
      .lookup<
        NativeFunction<
          Double Function(Int64, Double, Double, Pointer<NitroErrorFfi>)
        >
      >('benchmark_cpp_add')
      .asFunction<double Function(int, double, double, Pointer<NitroErrorFfi>)>(
        isLeaf: true,
      );
  late final double Function(int, double, double, Pointer<NitroErrorFfi>)
  _addFastPtr = _dylib
      .lookup<
        NativeFunction<
          Double Function(Int64, Double, Double, Pointer<NitroErrorFfi>)
        >
      >('benchmark_cpp_add_fast')
      .asFunction<double Function(int, double, double, Pointer<NitroErrorFfi>)>(
        isLeaf: true,
      );
  late final Pointer<Utf8> Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
  _getGreetingPtr = _dylib
      .lookupFunction<
        Pointer<Utf8> Function(Int64, Pointer<Utf8>, Pointer<NitroErrorFfi>),
        Pointer<Utf8> Function(int, Pointer<Utf8>, Pointer<NitroErrorFfi>)
      >('benchmark_cpp_get_greeting');
  late final int Function(int, Pointer<Uint8>, int, int, Pointer<NitroErrorFfi>)
  _hashBufferPtr = _dylib
      .lookupFunction<
        Int64 Function(
          Int64,
          Pointer<Uint8>,
          Size,
          Int64,
          Pointer<NitroErrorFfi>,
        ),
        int Function(int, Pointer<Uint8>, int, int, Pointer<NitroErrorFfi>)
      >('benchmark_cpp_hash_buffer');
  late final Pointer<Void> Function(
    int,
    Pointer<Void>,
    double,
    Pointer<NitroErrorFfi>,
  )
  _scalePointPtr = _dylib
      .lookupFunction<
        Pointer<Void> Function(
          Int64,
          Pointer<Void>,
          Double,
          Pointer<NitroErrorFfi>,
        ),
        Pointer<Void> Function(
          int,
          Pointer<Void>,
          double,
          Pointer<NitroErrorFfi>,
        )
      >('benchmark_cpp_scale_point');
  late final Pointer<Uint8> Function(int, int) _computeStatsPtr = _dylib
      .lookupFunction<
        Pointer<Uint8> Function(Int64, Int64),
        Pointer<Uint8> Function(int, int)
      >('benchmark_cpp_compute_stats');
  late final void Function(int, int, Pointer<NitroErrorFfi>, int)
  _computeStatsNativePtr = _dylib
      .lookupFunction<
        Void Function(Int64, Int64, Pointer<NitroErrorFfi>, Int64),
        void Function(int, int, Pointer<NitroErrorFfi>, int)
      >('benchmark_cpp_compute_stats_native');
  late final int Function(int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
  _sendLargeBufferFastPtr = _dylib
      .lookup<
        NativeFunction<
          Int64 Function(Int64, Pointer<Uint8>, Size, Pointer<NitroErrorFfi>)
        >
      >('benchmark_cpp_send_large_buffer_fast')
      .asFunction<
        int Function(int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
      >(isLeaf: true);
  late final int Function(int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
  _sendLargeBufferNoopPtr = _dylib
      .lookupFunction<
        Int64 Function(Int64, Pointer<Uint8>, Size, Pointer<NitroErrorFfi>),
        int Function(int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
      >('benchmark_cpp_send_large_buffer_noop');
  late final int Function(int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
  _sendLargeBufferNoopFastPtr = _dylib
      .lookup<
        NativeFunction<
          Int64 Function(Int64, Pointer<Uint8>, Size, Pointer<NitroErrorFfi>)
        >
      >('benchmark_cpp_send_large_buffer_noop_fast')
      .asFunction<
        int Function(int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
      >(isLeaf: true);
  late final int Function(int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
  _sendLargeBufferUnsafePtr = _dylib
      .lookupFunction<
        Int64 Function(Int64, Pointer<Uint8>, Int64, Pointer<NitroErrorFfi>),
        int Function(int, Pointer<Uint8>, int, Pointer<NitroErrorFfi>)
      >('benchmark_cpp_send_large_buffer_unsafe');
  late final void Function(int, int) _registerDataStreamPtr = _dylib
      .lookupFunction<Void Function(Int64, Int64), void Function(int, int)>(
        'benchmark_cpp_register_data_stream_stream',
      );
  late final void Function(int) _releaseDataStreamPtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'benchmark_cpp_release_data_stream_stream',
      );
  late final void Function(int, int) _registerBoxStreamPtr = _dylib
      .lookupFunction<Void Function(Int64, Int64), void Function(int, int)>(
        'benchmark_cpp_register_box_stream_stream',
      );
  late final void Function(int) _releaseBoxStreamPtr = _dylib
      .lookupFunction<Void Function(Int64), void Function(int)>(
        'benchmark_cpp_release_box_stream_stream',
      );
  // ignore: unused_field
  late final Pointer<NitroErrorFfi> Function() _getErrorPtr = _dylib
      .lookupFunction<
        Pointer<NitroErrorFfi> Function(),
        Pointer<NitroErrorFfi> Function()
      >('benchmark_cpp_get_error');
  // ignore: unused_field
  late final void Function() _clearErrorPtr = _dylib
      .lookupFunction<Void Function(), void Function()>(
        'benchmark_cpp_clear_error',
      );
  // ignore: unused_field
  late final Pointer<NativeFunction<Pointer<NitroErrorFfi> Function()>>
  _getErrorNativePtr = _dylib.lookup('benchmark_cpp_get_error');
  // ignore: unused_field
  late final Pointer<NativeFunction<Void Function()>> _clearErrorNativePtr =
      _dylib.lookup('benchmark_cpp_clear_error');

  @override
  void dispose() {
    if (isDisposed) return;
    NitroRuntime.logLifecycle(
      'dispose(benchmark_cpp)',
      'disposing (instanceId=$_instanceId)',
    );
    _destroyInstancePtr(_instanceId);
    NitroRuntime.releaseLib('benchmark_cpp');
    _instances.remove(_instanceKey);
    NitroInstanceRegistry.unregister(_instanceId, this);
    calloc.free(_nitroErr); // S8: free pre-allocated error slot
    super.dispose(); // sets isDisposed = true, calls onDestroy()
    NitroRuntime.logLifecycle('dispose(benchmark_cpp)', 'disposed');
  }

  /// Opaque reference to this native impl — pass to other plugins via AnyNativeObject.
  AnyNativeObject get asAnyNativeObject => AnyNativeObject(_instanceId);

  @override
  double add(double a, double b) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _addPtr(_instanceId, a, b, _nitroErr);
      NitroRuntime.throwIfOutParamError(_nitroErr);
      return res;
    }, methodName: 'add');
  }

  @override
  double addFast(double a, double b) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _addFastPtr(_instanceId, a, b, _nitroErr);
      return res;
    }, methodName: 'addFast');
  }

  @override
  String getGreeting(String name) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _getGreetingPtr(
          _instanceId,
          name.toNativeUtf8(allocator: arena),
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr);
        return res.toDartStringWithFree();
      }),
      methodName: 'getGreeting',
    );
  }

  @override
  int hashBuffer(Uint8List data, int rounds) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _hashBufferPtr(
          _instanceId,
          data.toPointer(arena),
          data.length,
          rounds,
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr);
        return res;
      }),
      methodName: 'hashBuffer',
    );
  }

  @override
  BenchmarkPoint scalePoint(BenchmarkPoint point, double factor) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _scalePointPtr(
          _instanceId,
          point.toNative(arena).cast<Void>(),
          factor,
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr);
        if (res == nullptr) {
          throw StateError('scalePoint returned null');
        }
        final structPtr = Pointer<BenchmarkPointFfi>.fromAddress(res.address);
        final BenchmarkPoint decoded;
        try {
          decoded = structPtr.ref.toDart();
        } finally {
          structPtr.ref.freeFields();
          malloc.free(structPtr);
        }
        return decoded;
      }),
      methodName: 'scalePoint',
    );
  }

  @override
  Future<BenchmarkStats> computeStats(int iterations) async {
    checkDisposed();
    final rawPtr = await NitroRuntime.callAsync<Pointer<Uint8>>(
      _computeStatsPtr,
      [_instanceId, iterations],
      getError: _getErrorNativePtr,
      clearError: _clearErrorNativePtr,
      methodName: 'computeStats',
    );
    final BenchmarkStats decoded;
    try {
      decoded = BenchmarkStatsRecordExt.fromNative(rawPtr);
    } finally {
      malloc.free(rawPtr);
    }
    return decoded;
  }

  @override
  Future<BenchmarkStats> computeStatsNative(int iterations) {
    checkDisposed();
    final _nitroErr = calloc<NitroErrorFfi>();
    return NitroRuntime.openNativeAsync<BenchmarkStats>(
      call: (port) =>
          _computeStatsNativePtr(_instanceId, iterations, _nitroErr, port),
      unpack: (raw) {
        NitroRuntime.throwIfOutParamErrorAndFree(_nitroErr);
        return ((raw) {
          final rawPtr = Pointer<Uint8>.fromAddress(raw as int);
          try {
            return BenchmarkStatsRecordExt.fromNative(rawPtr);
          } finally {
            malloc.free(rawPtr);
          }
        })(raw);
      },
      methodName: 'computeStatsNative',
    );
  }

  @override
  int sendLargeBufferFast(Uint8List buffer) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _sendLargeBufferFastPtr(
          _instanceId,
          buffer.toPointer(arena),
          buffer.length,
          _nitroErr,
        );
        return res;
      }),
      methodName: 'sendLargeBufferFast',
    );
  }

  @override
  int sendLargeBufferNoop(Uint8List buffer) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _sendLargeBufferNoopPtr(
          _instanceId,
          buffer.toPointer(arena),
          buffer.length,
          _nitroErr,
        );
        NitroRuntime.throwIfOutParamError(_nitroErr);
        return res;
      }),
      methodName: 'sendLargeBufferNoop',
    );
  }

  @override
  int sendLargeBufferNoopFast(Uint8List buffer) {
    checkDisposed();
    return NitroRuntime.callSync(
      () => withArena((arena) {
        final res = _sendLargeBufferNoopFastPtr(
          _instanceId,
          buffer.toPointer(arena),
          buffer.length,
          _nitroErr,
        );
        return res;
      }),
      methodName: 'sendLargeBufferNoopFast',
    );
  }

  @override
  int sendLargeBufferUnsafe(Pointer<Uint8> ptr, int length) {
    checkDisposed();
    return NitroRuntime.callSync(() {
      final res = _sendLargeBufferUnsafePtr(
        _instanceId,
        ptr,
        length,
        _nitroErr,
      );
      NitroRuntime.throwIfOutParamError(_nitroErr);
      return res;
    }, methodName: 'sendLargeBufferUnsafe');
  }

  @override
  Stream<BenchmarkPoint> get dataStream {
    checkDisposed();
    return NitroRuntime.openStream<BenchmarkPointProxy>(
      register: (port) => _registerDataStreamPtr(_instanceId, port),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream dataStream',
          );
        }
        return BenchmarkPointProxy(
          Pointer<BenchmarkPointFfi>.fromAddress(message as int),
        );
      },
      release: (port) => _releaseDataStreamPtr(port),
      backpressure: Backpressure.dropLatest,
    );
  }

  @override
  Stream<BenchmarkBox> get boxStream {
    checkDisposed();
    return NitroRuntime.openStream<BenchmarkBoxProxy>(
      register: (port) => _registerBoxStreamPtr(_instanceId, port),
      unpack: (message) {
        if (message == null) {
          throw StateError(
            'Received null event on non-nullable stream boxStream',
          );
        }
        return BenchmarkBoxProxy(
          Pointer<BenchmarkBoxFfi>.fromAddress(message as int),
        );
      },
      release: (port) => _releaseBoxStreamPtr(port),
      backpressure: Backpressure.dropLatest,
    );
  }
}

/// PX19: Creates the native (dart:ffi) implementation of [BenchmarkCpp].
/// On web, import `*.web.bridge.g.dart` conditionally and call
/// `createBenchmarkCppWebInstance()` instead.
BenchmarkCpp benchmarkCpp_createNativeInstance() => _BenchmarkCppImpl();

extension BenchmarkCppNativeRef on BenchmarkCpp {
  AnyNativeObject get asAnyNativeObject =>
      (this as _BenchmarkCppImpl).asAnyNativeObject;
}
