// Generated by Nitrogen Modules. Do not edit.
// Generated from: nitro_ar.native.dart
part of 'nitro_ar.native.dart';

// --- Struct FFI representations (generated by Nitrogen) ---

final class Vector3Ffi extends Struct {
  @Double()
  external double x;
  @Double()
  external double y;
  @Double()
  external double z;
}

extension Vector3FfiExt on Vector3Ffi {
  Vector3 toDart() {
    return Vector3(
      x: x,
      y: y,
      z: z,
    );
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  void freeFields() {}
}

extension Vector3Ext on Vector3 {
  Pointer<Vector3Ffi> toNative(Arena arena) {
    final ptr = arena<Vector3Ffi>();
    ptr.ref.x = x;
    ptr.ref.y = y;
    ptr.ref.z = z;
    return ptr;
  }
}

final class QuaternionFfi extends Struct {
  @Double()
  external double x;
  @Double()
  external double y;
  @Double()
  external double z;
  @Double()
  external double w;
}

extension QuaternionFfiExt on QuaternionFfi {
  Quaternion toDart() {
    return Quaternion(
      x: x,
      y: y,
      z: z,
      w: w,
    );
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  void freeFields() {}
}

extension QuaternionExt on Quaternion {
  Pointer<QuaternionFfi> toNative(Arena arena) {
    final ptr = arena<QuaternionFfi>();
    ptr.ref.x = x;
    ptr.ref.y = y;
    ptr.ref.z = z;
    ptr.ref.w = w;
    return ptr;
  }
}

final class BoundingBoxFfi extends Struct {
  @Double()
  external double x;
  @Double()
  external double y;
  @Double()
  external double width;
  @Double()
  external double height;
}

extension BoundingBoxFfiExt on BoundingBoxFfi {
  BoundingBox toDart() {
    return BoundingBox(
      x: x,
      y: y,
      width: width,
      height: height,
    );
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  void freeFields() {}
}

extension BoundingBoxExt on BoundingBox {
  Pointer<BoundingBoxFfi> toNative(Arena arena) {
    final ptr = arena<BoundingBoxFfi>();
    ptr.ref.x = x;
    ptr.ref.y = y;
    ptr.ref.width = width;
    ptr.ref.height = height;
    return ptr;
  }
}

final class PackageDimensionsFfi extends Struct {
  @Double()
  external double length;
  @Double()
  external double width;
  @Double()
  external double height;
  @Double()
  external double confidence;
  external Pointer<Vector3Ffi> center;
  external Pointer<QuaternionFfi> rotation;
}

extension PackageDimensionsFfiExt on PackageDimensionsFfi {
  PackageDimensions toDart() {
    return PackageDimensions(
      length: length,
      width: width,
      height: height,
      confidence: confidence,
      center: center.ref.toDart(),
      rotation: rotation.ref.toDart(),
    );
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  void freeFields() {
    if (center != nullptr) {
      center.ref.freeFields();
      malloc.free(center);
    }
    if (rotation != nullptr) {
      rotation.ref.freeFields();
      malloc.free(rotation);
    }
  }
}

extension PackageDimensionsExt on PackageDimensions {
  Pointer<PackageDimensionsFfi> toNative(Arena arena) {
    final ptr = arena<PackageDimensionsFfi>();
    ptr.ref.length = length;
    ptr.ref.width = width;
    ptr.ref.height = height;
    ptr.ref.confidence = confidence;
    ptr.ref.center = center.toNative(arena);
    ptr.ref.rotation = rotation.toNative(arena);
    return ptr;
  }
}

final class RawDepthMapFfi extends Struct {
  external Pointer<Uint8> data;
  @Int64()
  external int width;
  @Int64()
  external int height;
  @Int64()
  external int stride;
}

extension RawDepthMapFfiExt on RawDepthMapFfi {
  RawDepthMap toDart() {
    return RawDepthMap(
      data: Uint8List.fromList(data.asTypedList(stride)),
      width: width,
      height: height,
      stride: stride,
    );
  }

  /// Frees internal fields (like strings) that were allocated on the C heap.
  /// Does NOT free the struct itself.
  void freeFields() {}
}

extension RawDepthMapExt on RawDepthMap {
  Pointer<RawDepthMapFfi> toNative(Arena arena) {
    final ptr = arena<RawDepthMapFfi>();
    ptr.ref.data = data.toPointer(arena);
    ptr.ref.width = width;
    ptr.ref.height = height;
    ptr.ref.stride = stride;
    return ptr;
  }
}

// --- Struct Native Proxies (zero-copy, lazy field access) ---

/// Zero-copy proxy for [Vector3].
/// Extends [Vector3] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `Vector3Proxy <: Vector3`, a `Stream<Vector3Proxy>`
/// satisfies `Stream<Vector3>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'nitro_ar_release_Vector3'.
final class Vector3Proxy extends Vector3 implements Finalizable {
  final Pointer<Vector3Ffi> _native;

  static NativeFinalizer? _finalizer;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
          'nitro_ar_release_Vector3'),
    );
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] after passing the pointer here.
  Vector3Proxy(this._native) : super(x: 0.0, y: 0.0, z: 0.0) {
    assert(_finalizer != null,
        'Vector3Proxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.');
    _finalizer!.attach(this, _native.cast(), detach: this);
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  double get x => _native.ref.x;
  @override
  double get y => _native.ref.y;
  @override
  double get z => _native.ref.z;

  /// Eagerly copies all fields to a plain [Vector3] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  Vector3 toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    malloc.free(_native);
    return v;
  }
}

/// Zero-copy proxy for [Quaternion].
/// Extends [Quaternion] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `QuaternionProxy <: Quaternion`, a `Stream<QuaternionProxy>`
/// satisfies `Stream<Quaternion>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'nitro_ar_release_Quaternion'.
final class QuaternionProxy extends Quaternion implements Finalizable {
  final Pointer<QuaternionFfi> _native;

  static NativeFinalizer? _finalizer;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
          'nitro_ar_release_Quaternion'),
    );
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] after passing the pointer here.
  QuaternionProxy(this._native) : super(x: 0.0, y: 0.0, z: 0.0, w: 0.0) {
    assert(_finalizer != null,
        'QuaternionProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.');
    _finalizer!.attach(this, _native.cast(), detach: this);
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  double get x => _native.ref.x;
  @override
  double get y => _native.ref.y;
  @override
  double get z => _native.ref.z;
  @override
  double get w => _native.ref.w;

  /// Eagerly copies all fields to a plain [Quaternion] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  Quaternion toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    malloc.free(_native);
    return v;
  }
}

/// Zero-copy proxy for [BoundingBox].
/// Extends [BoundingBox] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `BoundingBoxProxy <: BoundingBox`, a `Stream<BoundingBoxProxy>`
/// satisfies `Stream<BoundingBox>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'nitro_ar_release_BoundingBox'.
final class BoundingBoxProxy extends BoundingBox implements Finalizable {
  final Pointer<BoundingBoxFfi> _native;

  static NativeFinalizer? _finalizer;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
          'nitro_ar_release_BoundingBox'),
    );
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] after passing the pointer here.
  BoundingBoxProxy(this._native)
      : super(x: 0.0, y: 0.0, width: 0.0, height: 0.0) {
    assert(_finalizer != null,
        'BoundingBoxProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.');
    _finalizer!.attach(this, _native.cast(), detach: this);
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  double get x => _native.ref.x;
  @override
  double get y => _native.ref.y;
  @override
  double get width => _native.ref.width;
  @override
  double get height => _native.ref.height;

  /// Eagerly copies all fields to a plain [BoundingBox] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  BoundingBox toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    malloc.free(_native);
    return v;
  }
}

/// Zero-copy proxy for [PackageDimensions].
/// Extends [PackageDimensions] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `PackageDimensionsProxy <: PackageDimensions`, a `Stream<PackageDimensionsProxy>`
/// satisfies `Stream<PackageDimensions>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'nitro_ar_release_PackageDimensions'.
final class PackageDimensionsProxy extends PackageDimensions
    implements Finalizable {
  final Pointer<PackageDimensionsFfi> _native;

  static NativeFinalizer? _finalizer;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
          'nitro_ar_release_PackageDimensions'),
    );
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] after passing the pointer here.
  PackageDimensionsProxy(this._native)
      : super(
            length: 0.0,
            width: 0.0,
            height: 0.0,
            confidence: 0.0,
            center: Vector3(x: 0.0, y: 0.0, z: 0.0),
            rotation: Quaternion(x: 0.0, y: 0.0, z: 0.0, w: 0.0)) {
    assert(_finalizer != null,
        'PackageDimensionsProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.');
    _finalizer!.attach(this, _native.cast(), detach: this);
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  double get length => _native.ref.length;
  @override
  double get width => _native.ref.width;
  @override
  double get height => _native.ref.height;
  @override
  double get confidence => _native.ref.confidence;
  @override
  Vector3 get center => _native.ref.center.ref.toDart();
  @override
  Quaternion get rotation => _native.ref.rotation.ref.toDart();

  /// Eagerly copies all fields to a plain [PackageDimensions] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  PackageDimensions toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    malloc.free(_native);
    return v;
  }
}

/// Zero-copy proxy for [RawDepthMap].
/// Extends [RawDepthMap] and overrides every getter to read lazily from
/// native memory — no field is copied at construction time.
/// Because `RawDepthMapProxy <: RawDepthMap`, a `Stream<RawDepthMapProxy>`
/// satisfies `Stream<RawDepthMap>` via Dart covariant generics.
/// Native memory is freed via a [NativeFinalizer] backed by the
/// generated C symbol 'nitro_ar_release_RawDepthMap'.
final class RawDepthMapProxy extends RawDepthMap implements Finalizable {
  final Pointer<RawDepthMapFfi> _native;

  static NativeFinalizer? _finalizer;

  /// Binds the generated release symbol from [dylib].
  /// Must be called once — typically in the impl class constructor.
  /// Idempotent: subsequent calls are a no-op.
  static void _init(DynamicLibrary dylib) {
    _finalizer ??= NativeFinalizer(
      dylib.lookup<NativeFunction<Void Function(Pointer<Void>)>>(
          'nitro_ar_release_RawDepthMap'),
    );
  }

  /// Takes ownership of [native]. Super fields are zeroed and never read;
  /// all getters below are overridden to read from native memory instead.
  /// Do NOT call [malloc.free] after passing the pointer here.
  RawDepthMapProxy(this._native)
      : super(data: Uint8List(0), width: 0, height: 0, stride: 0) {
    assert(_finalizer != null,
        'RawDepthMapProxy._init() was not called. Ensure the Nitro impl class constructor ran before creating proxies.');
    _finalizer!.attach(this, _native.cast(), detach: this);
  }

  // @override lazy getters — read native memory on demand, zero allocation.
  @override
  Uint8List get data => _native.ref.data.asTypedList(_native.ref.stride);
  @override
  int get width => _native.ref.width;
  @override
  int get height => _native.ref.height;
  @override
  int get stride => _native.ref.stride;

  /// Eagerly copies all fields to a plain [RawDepthMap] value, detaches
  /// the finalizer, and frees native memory immediately.
  /// Use this only when you need an immutable snapshot; for streams
  /// prefer consuming the proxy fields lazily then letting it GC.
  /// Must not be called more than once.
  RawDepthMap toDartAndRelease() {
    final v = _native.ref.toDart();
    _finalizer?.detach(this);
    malloc.free(_native);
    return v;
  }
}

class _NitroArImpl extends NitroAr {
  final DynamicLibrary _dylib;

  _NitroArImpl() : _dylib = NitroRuntime.loadLib('nitro_ar') {
    final initFunc = _dylib.lookupFunction<IntPtr Function(Pointer<Void>),
        int Function(Pointer<Void>)>('nitro_ar_init_dart_api_dl');
    final initCode = initFunc(NativeApi.initializeApiDLData);
    if (initCode != 0) {
      throw StateError(
          'nitro_ar: Dart API DL initialization failed with code $initCode.');
    }
    Vector3Proxy._init(_dylib);
    QuaternionProxy._init(_dylib);
    BoundingBoxProxy._init(_dylib);
    PackageDimensionsProxy._init(_dylib);
    RawDepthMapProxy._init(_dylib);
  }

  late final double Function(double, double) _addPtr = _dylib
      .lookup<NativeFunction<Double Function(Double, Double)>>('nitro_ar_add')
      .asFunction<double Function(double, double)>(isLeaf: true);
  late final Pointer<Utf8> Function(Pointer<Utf8>) _getGreetingPtr =
      _dylib.lookupFunction<Pointer<Utf8> Function(Pointer<Utf8>),
          Pointer<Utf8> Function(Pointer<Utf8>)>('nitro_ar_get_greeting');
  late final int Function() _isDepthSupportedPtr = _dylib
      .lookup<NativeFunction<Int8 Function()>>('nitro_ar_is_depth_supported')
      .asFunction<int Function()>(isLeaf: true);
  late final Pointer<Void> Function(Pointer<Void>) _detectPackagePtr =
      _dylib.lookupFunction<Pointer<Void> Function(Pointer<Void>),
          Pointer<Void> Function(Pointer<Void>)>('nitro_ar_detect_package');
  late final Pointer<Void> Function() _getRawDepthMapPtr =
      _dylib.lookupFunction<Pointer<Void> Function(), Pointer<Void> Function()>(
          'nitro_ar_get_raw_depth_map');
  late final double Function(Pointer<Utf8>) _estimateVolumePtr =
      _dylib.lookupFunction<Double Function(Pointer<Utf8>),
          double Function(Pointer<Utf8>)>('nitro_ar_estimate_volume');
  // ignore: unused_field
  late final Pointer<NitroErrorFfi> Function() _getErrorPtr =
      _dylib.lookupFunction<Pointer<NitroErrorFfi> Function(),
          Pointer<NitroErrorFfi> Function()>('nitro_ar_get_error');
  // ignore: unused_field
  late final void Function() _clearErrorPtr = _dylib
      .lookupFunction<Void Function(), void Function()>('nitro_ar_clear_error');
  // ignore: unused_field
  late final Pointer<NativeFunction<Pointer<NitroErrorFfi> Function()>>
      _getErrorNativePtr = _dylib.lookup('nitro_ar_get_error');
  // ignore: unused_field
  late final Pointer<NativeFunction<Void Function()>> _clearErrorNativePtr =
      _dylib.lookup('nitro_ar_clear_error');

  @override
  // ignore: unnecessary_overrides
  void dispose() {
    super.dispose(); // sets isDisposed = true, calls onDestroy()
  }

  @override
  double add(double a, double b) {
    checkDisposed();
    final res = _addPtr(a, b);
    NitroRuntime.checkError(_getErrorPtr, _clearErrorPtr);
    return res;
  }

  @override
  Future<String> getGreeting(String name) async {
    checkDisposed();
    final arena = Arena();
    try {
      final rawPtr = await NitroRuntime.callAsync<Pointer<Utf8>>(
          _getGreetingPtr, [name.toNativeUtf8(allocator: arena)],
          getError: _getErrorNativePtr,
          clearError: _clearErrorNativePtr,
          methodName: 'getGreeting');
      return rawPtr.toDartStringWithFree();
    } finally {
      arena.releaseAll();
    }
  }

  @override
  bool isDepthSupported() {
    checkDisposed();
    final res = _isDepthSupportedPtr();
    NitroRuntime.checkError(_getErrorPtr, _clearErrorPtr);
    return res != 0;
  }

  @override
  PackageDimensions detectPackage(BoundingBox rect) {
    checkDisposed();
    return withArena((arena) {
      final res = _detectPackagePtr(rect.toNative(arena).cast<Void>());
      NitroRuntime.checkError(_getErrorPtr, _clearErrorPtr);
      final structPtr = Pointer<PackageDimensionsFfi>.fromAddress(res.address);
      final PackageDimensions decoded;
      try {
        decoded = structPtr.ref.toDart();
      } finally {
        structPtr.ref.freeFields();
        malloc.free(structPtr);
      }
      return decoded;
    });
  }

  @override
  RawDepthMap getRawDepthMap() {
    checkDisposed();
    final res = _getRawDepthMapPtr();
    NitroRuntime.checkError(_getErrorPtr, _clearErrorPtr);
    final structPtr = Pointer<RawDepthMapFfi>.fromAddress(res.address);
    final RawDepthMap decoded;
    try {
      decoded = structPtr.ref.toDart();
    } finally {
      structPtr.ref.freeFields();
      malloc.free(structPtr);
    }
    return decoded;
  }

  @override
  double estimateVolume(String anchor) {
    checkDisposed();
    return withArena((arena) {
      final res = _estimateVolumePtr(anchor.toNativeUtf8(allocator: arena));
      NitroRuntime.checkError(_getErrorPtr, _clearErrorPtr);
      return res;
    });
  }
}
