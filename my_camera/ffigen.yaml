# Run with `dart run ffigen --config ffigen.yaml`.
name: MyCameraBindings
description: |
  Bindings for `src/my_camera.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/my_camera_bindings_generated.dart'
headers:
  entry-points:
    - 'src/my_camera.h'
  include-directives:
    - 'src/my_camera.h'
comments:
  style: any
  length: full
