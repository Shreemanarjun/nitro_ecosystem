// Generated by Nitrogen Modules. Do not edit.
// Generated from: nitro_camera.native.dart
import Foundation
import Combine

// --- Enums ---
public enum CameraPosition: Int64 {
  case front = 0
  case back = 1
  case external = 2
}

public enum CameraLensType: Int64 {
  case unknown = 0
  case wideAngle = 1
  case ultraWideAngle = 2
  case telephoto = 3
}

public enum FlashMode: Int64 {
  case off = 0
  case on = 1
  case auto = 2
}

public enum AutoFocusMode: Int64 {
  case off = 0
  case continuous = 1
  case locked = 2
}

public enum PermissionStatus: Int64 {
  case notDetermined = 0
  case granted = 1
  case denied = 2
  case restricted = 3
}

// --- Structs ---
public struct CameraFrame {
  public var pixels: UnsafeMutablePointer<UInt8>?
  public var size: Int64
  public var width: Int64
  public var height: Int64
  public var timestamp: Int64
  public var orientation: Int64
  public var textureId: Int64
}

// --- @HybridRecord binary extensions (generated by Nitrogen) ---

public struct CameraDevice {
  public var id: String
  public var name: String
  public var position: Int64
  public var lensType: Int64
  public var sensorOrientation: Int64
  public var minZoom: Double
  public var maxZoom: Double
  public var neutralZoom: Double
  public var hasFlash: Int64
  public var hasTorch: Int64
  public var maxPhotoWidth: Int64
  public var maxPhotoHeight: Int64

  public init(id: String, name: String, position: Int64, lensType: Int64, sensorOrientation: Int64, minZoom: Double, maxZoom: Double, neutralZoom: Double, hasFlash: Int64, hasTorch: Int64, maxPhotoWidth: Int64, maxPhotoHeight: Int64) {
    self.id = id
    self.name = name
    self.position = position
    self.lensType = lensType
    self.sensorOrientation = sensorOrientation
    self.minZoom = minZoom
    self.maxZoom = maxZoom
    self.neutralZoom = neutralZoom
    self.hasFlash = hasFlash
    self.hasTorch = hasTorch
    self.maxPhotoWidth = maxPhotoWidth
    self.maxPhotoHeight = maxPhotoHeight
  }

  public static func fromNative(_ ptr: UnsafeMutablePointer<UInt8>) -> CameraDevice {
    return fromReader(NitroRecordReader(ptr: ptr))
  }

  public static func fromReader(_ r: NitroRecordReader) -> CameraDevice {
    return CameraDevice(
      id: r.readString(),
      name: r.readString(),
      position: r.readInt(),
      lensType: r.readInt(),
      sensorOrientation: r.readInt(),
      minZoom: r.readDouble(),
      maxZoom: r.readDouble(),
      neutralZoom: r.readDouble(),
      hasFlash: r.readInt(),
      hasTorch: r.readInt(),
      maxPhotoWidth: r.readInt(),
      maxPhotoHeight: r.readInt(),
    )
  }

  public func writeFields(_ w: NitroRecordWriter) {
    w.writeString(id)
    w.writeString(name)
    w.writeInt(position)
    w.writeInt(lensType)
    w.writeInt(sensorOrientation)
    w.writeDouble(minZoom)
    w.writeDouble(maxZoom)
    w.writeDouble(neutralZoom)
    w.writeInt(hasFlash)
    w.writeInt(hasTorch)
    w.writeInt(maxPhotoWidth)
    w.writeInt(maxPhotoHeight)
  }

  public func toNative() -> UnsafeMutablePointer<UInt8>? {
    let w = NitroRecordWriter()
    writeFields(w)
    return w.toNative()
  }
}

public struct PhotoResult {
  public var path: String
  public var width: Int64
  public var height: Int64
  public var fileSize: Int64

  public init(path: String, width: Int64, height: Int64, fileSize: Int64) {
    self.path = path
    self.width = width
    self.height = height
    self.fileSize = fileSize
  }

  public static func fromNative(_ ptr: UnsafeMutablePointer<UInt8>) -> PhotoResult {
    return fromReader(NitroRecordReader(ptr: ptr))
  }

  public static func fromReader(_ r: NitroRecordReader) -> PhotoResult {
    return PhotoResult(
      path: r.readString(),
      width: r.readInt(),
      height: r.readInt(),
      fileSize: r.readInt(),
    )
  }

  public func writeFields(_ w: NitroRecordWriter) {
    w.writeString(path)
    w.writeInt(width)
    w.writeInt(height)
    w.writeInt(fileSize)
  }

  public func toNative() -> UnsafeMutablePointer<UInt8>? {
    let w = NitroRecordWriter()
    writeFields(w)
    return w.toNative()
  }
}

public struct RecordingResult {
  public var path: String
  public var durationMs: Int64
  public var fileSize: Int64

  public init(path: String, durationMs: Int64, fileSize: Int64) {
    self.path = path
    self.durationMs = durationMs
    self.fileSize = fileSize
  }

  public static func fromNative(_ ptr: UnsafeMutablePointer<UInt8>) -> RecordingResult {
    return fromReader(NitroRecordReader(ptr: ptr))
  }

  public static func fromReader(_ r: NitroRecordReader) -> RecordingResult {
    return RecordingResult(
      path: r.readString(),
      durationMs: r.readInt(),
      fileSize: r.readInt(),
    )
  }

  public func writeFields(_ w: NitroRecordWriter) {
    w.writeString(path)
    w.writeInt(durationMs)
    w.writeInt(fileSize)
  }

  public func toNative() -> UnsafeMutablePointer<UInt8>? {
    let w = NitroRecordWriter()
    writeFields(w)
    return w.toNative()
  }
}

public class NitroRecordWriter {
    public var bytes: [UInt8] = []
    public init() {}
    public func writeInt(_ v: Int64) {
        var val = v.littleEndian
        withUnsafeBytes(of: &val) { bytes.append(contentsOf: $0) }
    }
    public func writeInt32(_ v: Int32) {
        var val = v.littleEndian
        withUnsafeBytes(of: &val) { bytes.append(contentsOf: $0) }
    }
    public func writeDouble(_ v: Double) {
        var val = v.bitPattern.littleEndian
        withUnsafeBytes(of: &val) { bytes.append(contentsOf: $0) }
    }
    public func writeBool(_ v: Bool) {
        bytes.append(v ? 1 : 0)
    }
    public func writeString(_ v: String) {
        let utf8 = Array(v.utf8)
        writeInt32(Int32(utf8.count))
        bytes.append(contentsOf: utf8)
    }
    public func writeNullTag(_ isNull: Bool) {
        bytes.append(isNull ? 0 : 1)
    }
    public func toNative() -> UnsafeMutablePointer<UInt8>? {
        let total = 4 + bytes.count
        let ptr = UnsafeMutablePointer<UInt8>.allocate(capacity: total)
        var length = Int32(bytes.count).littleEndian
        withUnsafeBytes(of: &length) { 
            ptr.update(from: $0.baseAddress!.assumingMemoryBound(to: UInt8.self), count: 4)
        }
        ptr.advanced(by: 4).update(from: bytes, count: bytes.count)
        return ptr
    }
    public static func encodeList<T>(_ items: [T], writeItem: (NitroRecordWriter, T) -> Void) -> UnsafeMutablePointer<UInt8>? {
        let w = NitroRecordWriter()
        w.writeInt32(Int32(items.count))
        for item in items { writeItem(w, item) }
        return w.toNative()
    }
}

public class NitroRecordReader {
    public let bytes: UnsafeMutablePointer<UInt8>
    public var pos: Int = 0
    public init(ptr: UnsafeMutablePointer<UInt8>) {
        self.bytes = ptr.advanced(by: 4)
    }
    public init(payload: UnsafeMutablePointer<UInt8>) {
        self.bytes = payload
    }
    public func readInt() -> Int64 {
        let v = UnsafeRawPointer(bytes.advanced(by: pos)).load(as: Int64.self)
        pos += 8
        return Int64(littleEndian: v)
    }
    public func readInt32() -> Int32 {
        let v = UnsafeRawPointer(bytes.advanced(by: pos)).load(as: Int32.self)
        pos += 4
        return Int32(littleEndian: v)
    }
    public func readDouble() -> Double {
        let v = UnsafeRawPointer(bytes.advanced(by: pos)).load(as: UInt64.self)
        pos += 8
        return Double(bitPattern: UInt64(littleEndian: v))
    }
    public func readBool() -> Bool {
        let v = bytes[pos]
        pos += 1
        return v != 0
    }
    public func readString() -> String {
        let len = Int(readInt32())
        let s = String(bytesNoCopy: UnsafeMutableRawPointer(bytes.advanced(by: pos)), length: len, encoding: .utf8, freeWhenDone: false) ?? ""
        pos += len
        return s
    }
    public func readNullTag() -> Bool {
        let v = bytes[pos]
        pos += 1
        return v == 0
    }
    public static func decodeList<T>(_ ptr: UnsafeMutablePointer<UInt8>, readItem: (NitroRecordReader) -> T) -> [T] {
        let r = NitroRecordReader(ptr: ptr)
        let count = r.readInt32()
        var items: [T] = []
        for _ in 0..<count {
            items.append(readItem(r))
        }
        return items
    }
}

/**
 * Protocol for the NitroCamera module.
 * Conform to this in your Swift source code.
 */
public protocol HybridNitroCameraProtocol: AnyObject {
    func requestCameraPermission() async throws -> Int64
    func getCameraPermissionStatus() async throws -> Int64
    func requestMicrophonePermission() async throws -> Int64
    func getMicrophonePermissionStatus() async throws -> Int64
    func getAvailableCameraDevicesJson() async throws -> String
    func getDeviceCount() async throws -> Int64
    func getDevice(index: Int64) async throws -> CameraDevice
    func openCamera(deviceId: String, width: Int64, height: Int64, fps: Int64, enableAudio: Int64) async throws -> Int64
    func closeCamera(textureId: Int64) async throws -> Void
    func startPreview(textureId: Int64) async throws -> Void
    func stopPreview(textureId: Int64) async throws -> Void
    func setZoom(textureId: Int64, zoom: Double) async throws -> Void
    func setFocusPoint(textureId: Int64, x: Double, y: Double) async throws -> Void
    func setAutoFocus(textureId: Int64, mode: Int64) async throws -> Void
    func setExposure(textureId: Int64, value: Double) async throws -> Void
    func setFlash(textureId: Int64, mode: Int64) async throws -> Void
    func setTorch(textureId: Int64, enabled: Int64) async throws -> Void
    func setWhiteBalance(textureId: Int64, temperature: Int64) async throws -> Void
    func setHdr(textureId: Int64, enabled: Int64) async throws -> Void
    func takePhoto(textureId: Int64) async throws -> PhotoResult
    func startVideoRecording(textureId: Int64, outputPath: String) async throws -> Void
    func stopVideoRecording(textureId: Int64) async throws -> RecordingResult
    func pauseRecording(textureId: Int64) async throws -> Void
    func resumeRecording(textureId: Int64) async throws -> Void
    func cancelRecording(textureId: Int64) async throws -> Void
    func enableFrameProcessing(textureId: Int64, enabled: Int64) async throws -> Void
    func setFrameFormat(textureId: Int64, format: Int64) async throws -> Void
    func setFilterShader(textureId: Int64, shaderSource: String) async throws -> Void
    func updateOverlay(textureId: Int64, overlayData: String) async throws -> Void
    var frameStream: AnyPublisher<CameraFrame, Never> { get }
}

public class NitroCameraRegistry {
    public static var impl: HybridNitroCameraProtocol?

    public static func register(_ impl: HybridNitroCameraProtocol) {
        NitroCameraRegistry.impl = impl
    }

    // Stream: frameStream cancellables keyed by dartPort
    public static var _frameStreamCancellables = [Int64: AnyCancellable]()
}

// MARK: - C bridge stubs — exported as C symbols called by the generated .cpp shim

@_cdecl("NitroSwift_nitro_camera_request_camera_permission")
public func NitroSwift_nitro_camera_request_camera_permission() -> Int64 {
    guard let impl = NitroCameraRegistry.impl else { return 0 }
    let sema = DispatchSemaphore(value: 0)
    var result: Int64? = nil
    Task.detached {
        result = try? await impl.requestCameraPermission()
        sema.signal()
    }
    sema.wait()
    return result ?? 0
}

@_cdecl("NitroSwift_nitro_camera_get_camera_permission_status")
public func NitroSwift_nitro_camera_get_camera_permission_status() -> Int64 {
    guard let impl = NitroCameraRegistry.impl else { return 0 }
    let sema = DispatchSemaphore(value: 0)
    var result: Int64? = nil
    Task.detached {
        result = try? await impl.getCameraPermissionStatus()
        sema.signal()
    }
    sema.wait()
    return result ?? 0
}

@_cdecl("NitroSwift_nitro_camera_request_microphone_permission")
public func NitroSwift_nitro_camera_request_microphone_permission() -> Int64 {
    guard let impl = NitroCameraRegistry.impl else { return 0 }
    let sema = DispatchSemaphore(value: 0)
    var result: Int64? = nil
    Task.detached {
        result = try? await impl.requestMicrophonePermission()
        sema.signal()
    }
    sema.wait()
    return result ?? 0
}

@_cdecl("NitroSwift_nitro_camera_get_microphone_permission_status")
public func NitroSwift_nitro_camera_get_microphone_permission_status() -> Int64 {
    guard let impl = NitroCameraRegistry.impl else { return 0 }
    let sema = DispatchSemaphore(value: 0)
    var result: Int64? = nil
    Task.detached {
        result = try? await impl.getMicrophonePermissionStatus()
        sema.signal()
    }
    sema.wait()
    return result ?? 0
}

@_cdecl("NitroSwift_nitro_camera_get_available_camera_devices_json")
public func NitroSwift_nitro_camera_get_available_camera_devices_json() -> UnsafeMutablePointer<CChar>? {
    guard let impl = NitroCameraRegistry.impl else { return strdup("") }
    let sema = DispatchSemaphore(value: 0)
    var result = ""
    Task.detached {
        result = (try? await impl.getAvailableCameraDevicesJson()) ?? ""
        sema.signal()
    }
    sema.wait()
    return strdup(result)
}

@_cdecl("NitroSwift_nitro_camera_get_device_count")
public func NitroSwift_nitro_camera_get_device_count() -> Int64 {
    guard let impl = NitroCameraRegistry.impl else { return 0 }
    let sema = DispatchSemaphore(value: 0)
    var result: Int64? = nil
    Task.detached {
        result = try? await impl.getDeviceCount()
        sema.signal()
    }
    sema.wait()
    return result ?? 0
}

@_cdecl("NitroSwift_nitro_camera_get_device")
public func NitroSwift_nitro_camera_get_device(_ index: Int64) -> UnsafeMutablePointer<UInt8>? {
    guard let impl = NitroCameraRegistry.impl else { return nil }
    let sema = DispatchSemaphore(value: 0)
    var result: CameraDevice? = nil
    Task.detached {
        result = try? await impl.getDevice(index: index)
        sema.signal()
    }
    sema.wait()
    return result?.toNative()
}

@_cdecl("NitroSwift_nitro_camera_open_camera")
public func NitroSwift_nitro_camera_open_camera(_ deviceId: UnsafePointer<CChar>?, _ width: Int64, _ height: Int64, _ fps: Int64, _ enableAudio: Int64) -> Int64 {
    let deviceIdStr = deviceId.map { String(cString: $0) } ?? ""
    guard let impl = NitroCameraRegistry.impl else { return 0 }
    let sema = DispatchSemaphore(value: 0)
    var result: Int64? = nil
    Task.detached {
        result = try? await impl.openCamera(deviceId: deviceIdStr, width: width, height: height, fps: fps, enableAudio: enableAudio)
        sema.signal()
    }
    sema.wait()
    return result ?? 0
}

@_cdecl("NitroSwift_nitro_camera_close_camera")
public func NitroSwift_nitro_camera_close_camera(_ textureId: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.closeCamera(textureId: textureId)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_start_preview")
public func NitroSwift_nitro_camera_start_preview(_ textureId: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.startPreview(textureId: textureId)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_stop_preview")
public func NitroSwift_nitro_camera_stop_preview(_ textureId: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.stopPreview(textureId: textureId)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_zoom")
public func NitroSwift_nitro_camera_set_zoom(_ textureId: Int64, _ zoom: Double) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setZoom(textureId: textureId, zoom: zoom)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_focus_point")
public func NitroSwift_nitro_camera_set_focus_point(_ textureId: Int64, _ x: Double, _ y: Double) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setFocusPoint(textureId: textureId, x: x, y: y)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_auto_focus")
public func NitroSwift_nitro_camera_set_auto_focus(_ textureId: Int64, _ mode: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setAutoFocus(textureId: textureId, mode: mode)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_exposure")
public func NitroSwift_nitro_camera_set_exposure(_ textureId: Int64, _ value: Double) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setExposure(textureId: textureId, value: value)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_flash")
public func NitroSwift_nitro_camera_set_flash(_ textureId: Int64, _ mode: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setFlash(textureId: textureId, mode: mode)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_torch")
public func NitroSwift_nitro_camera_set_torch(_ textureId: Int64, _ enabled: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setTorch(textureId: textureId, enabled: enabled)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_white_balance")
public func NitroSwift_nitro_camera_set_white_balance(_ textureId: Int64, _ temperature: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setWhiteBalance(textureId: textureId, temperature: temperature)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_hdr")
public func NitroSwift_nitro_camera_set_hdr(_ textureId: Int64, _ enabled: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setHdr(textureId: textureId, enabled: enabled)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_take_photo")
public func NitroSwift_nitro_camera_take_photo(_ textureId: Int64) -> UnsafeMutablePointer<UInt8>? {
    guard let impl = NitroCameraRegistry.impl else { return nil }
    let sema = DispatchSemaphore(value: 0)
    var result: PhotoResult? = nil
    Task.detached {
        result = try? await impl.takePhoto(textureId: textureId)
        sema.signal()
    }
    sema.wait()
    return result?.toNative()
}

@_cdecl("NitroSwift_nitro_camera_start_video_recording")
public func NitroSwift_nitro_camera_start_video_recording(_ textureId: Int64, _ outputPath: UnsafePointer<CChar>?) -> Void {
    let outputPathStr = outputPath.map { String(cString: $0) } ?? ""
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.startVideoRecording(textureId: textureId, outputPath: outputPathStr)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_stop_video_recording")
public func NitroSwift_nitro_camera_stop_video_recording(_ textureId: Int64) -> UnsafeMutablePointer<UInt8>? {
    guard let impl = NitroCameraRegistry.impl else { return nil }
    let sema = DispatchSemaphore(value: 0)
    var result: RecordingResult? = nil
    Task.detached {
        result = try? await impl.stopVideoRecording(textureId: textureId)
        sema.signal()
    }
    sema.wait()
    return result?.toNative()
}

@_cdecl("NitroSwift_nitro_camera_pause_recording")
public func NitroSwift_nitro_camera_pause_recording(_ textureId: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.pauseRecording(textureId: textureId)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_resume_recording")
public func NitroSwift_nitro_camera_resume_recording(_ textureId: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.resumeRecording(textureId: textureId)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_cancel_recording")
public func NitroSwift_nitro_camera_cancel_recording(_ textureId: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.cancelRecording(textureId: textureId)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_enable_frame_processing")
public func NitroSwift_nitro_camera_enable_frame_processing(_ textureId: Int64, _ enabled: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.enableFrameProcessing(textureId: textureId, enabled: enabled)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_frame_format")
public func NitroSwift_nitro_camera_set_frame_format(_ textureId: Int64, _ format: Int64) -> Void {
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setFrameFormat(textureId: textureId, format: format)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_set_filter_shader")
public func NitroSwift_nitro_camera_set_filter_shader(_ textureId: Int64, _ shaderSource: UnsafePointer<CChar>?) -> Void {
    let shaderSourceStr = shaderSource.map { String(cString: $0) } ?? ""
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.setFilterShader(textureId: textureId, shaderSource: shaderSourceStr)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("NitroSwift_nitro_camera_update_overlay")
public func NitroSwift_nitro_camera_update_overlay(_ textureId: Int64, _ overlayData: UnsafePointer<CChar>?) -> Void {
    let overlayDataStr = overlayData.map { String(cString: $0) } ?? ""
    guard let impl = NitroCameraRegistry.impl else { return }
    let sema = DispatchSemaphore(value: 0)
    Task.detached {
        try? await impl.updateOverlay(textureId: textureId, overlayData: overlayDataStr)
        sema.signal()
    }
    sema.wait()
}

@_cdecl("_register_frameStream_stream")
public func _register_frameStream_stream(
    _ dartPort: Int64,
    _ emitCb: @convention(c) (Int64, UnsafeMutableRawPointer?) -> Void
) {
    NitroCameraRegistry._frameStreamCancellables[dartPort] =
        NitroCameraRegistry.impl?.frameStream.sink { item in
            let ptr = UnsafeMutablePointer<CameraFrame>.allocate(capacity: 1)
            ptr.initialize(to: item)
            emitCb(dartPort, UnsafeMutableRawPointer(ptr))
        }
}

@_cdecl("_release_frameStream_stream")
public func _release_frameStream_stream(_ dartPort: Int64) {
    NitroCameraRegistry._frameStreamCancellables[dartPort]?.cancel()
    NitroCameraRegistry._frameStreamCancellables.removeValue(forKey: dartPort)
}
